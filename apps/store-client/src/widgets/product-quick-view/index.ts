// Product quick-view widget (TASK-086) — an eye-icon trigger that opens a modal
// preview of a product. The dialog + skeleton are internal; only the trigger is
// exported (widgets inject it into ProductCard's `hoverAction` slot).
export { ProductQuickViewTrigger } from "./ui/product-quick-view-trigger";
