// The shared numbered pagination (TASK-417). Imported as
// `@/shared/ui/pagination` rather than through the `shared/ui` barrel, so a
// listing page pulls in one small control instead of the whole UI kit.
export { Pagination } from "./pagination";
