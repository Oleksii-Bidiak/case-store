// Google Merchant Center feed builder (plan 148) — pure XML generation for
// app/merchant-feed.xml/route.ts.
export { escapeXml } from "./escapeXml";
export { buildMerchantFeedXml, DESCRIPTION_MAX } from "./buildMerchantFeedXml";
export type {
  MerchantFeedProduct,
  BuildMerchantFeedXmlInput,
} from "./buildMerchantFeedXml";
