// Content-map widget («Де що на сайті», TASK-264) — the storefront→admin
// orientation schematic. Only the root view is public; the zone-card and
// page-group are internal composition details.
export { ContentMapView } from "./ui/content-map-view";
