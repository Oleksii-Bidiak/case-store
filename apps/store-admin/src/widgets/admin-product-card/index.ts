// Read-only product card (TASK-427) — everything the edit form shows, without
// the inputs, plus the change history (owner-only, see ProductHistoryPanel).
export { AdminProductCardView } from "./ui/admin-product-card-view";
export { ProductHistoryPanel } from "./ui/product-history-panel";
