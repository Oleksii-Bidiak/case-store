// Product delete (TASK-427) — the soft-delete action and its confirm dialog,
// shared by the product list row, the edit page and the read-only card.
export { ProductDeleteAction } from "./ui/product-delete-action";
