# Project Backlog

> **Single source of truth** for task status.
> Completed phases are summarized below (one row per parent); full sub-task history lives
> in [`docs/backlog-archive.md`](docs/backlog-archive.md). Open work is in **Roadmap (Open)**.
> When you don't know what to do, ask: "What's next?" — pick the first ⬜/🔄 in **Tier 0**, then
> Tier 1, 2, 3, 4. Tiers are ordered **critical → least important**; localization (Tier 1) is the
> owner's headline priority, sitting just below the data/security bugs that corrupt real data.
> Skip the **Logic behind imported UI** section when picking "what's next" — it is deferred until
> the UI import (TASK-167) is fully ported, per owner decision.

## Status Legend

| Symbol | Meaning |
| --- | --- |
| ⬜ | To Do — not started |
| 🔄 | In Progress — actively being worked on |
| ✅ | Done — build/lint/typecheck + automated tests green |
| ❌ | Blocked |
| 🅿️ | Parked — tracked but intentionally not in the active sequence |

> A task is **✅** once build/lint/typecheck and automated tests pass. "Manual visual QA on
> a running stack" does **not** hold a task open — those few items live in *Pending manual QA*.

---

## Pending manual QA

> Code shipped and automated gates green; these need a one-off check on a running stack.

| Item | From | Note |
| --- | --- | --- |
| Real-Redis cache hit/miss/invalidation int run | TASK-044-I | needs Docker Redis up |
| Dashboard raw-SQL real-DB int-spec run — now un-staled (TASK-152: rewritten to current schema, `paymentStatus=PAID` revenue + top-products + low-stock assertions); run `npm run test:int -w apps/store-api` against `store_test`. Also visually confirm the dashboard shows the Top-products table and the low-stock table on a running stack | TASK-066 / TASK-152 | needs `test:int` DB (no local Postgres at implementation time) |
| JSON-LD present in product HTML | TASK-045-I | needs live API |
| Admin login + silent refresh + CSRF path smoke | TASK-059-B / TASK-112 | refresh-path bug now fixed; live smoke still pending a running stack |
| Checkout end-to-end (UA form) + order-confirmation render | TASK-111 → TASK-119 | QA on HEAD `296b498` shows order created but redirect lands on empty `/cart`; reopened as bug TASK-119. TASK-111 absorbed into TASK-119 (plan 053) — see Manual QA Checklist in that plan |
| Run Playwright E2E (`npm run test:e2e:pw`) — 4 specs discovered; need DB + `npx playwright install chromium` + booted API/client to execute | TASK-105-D | scaffold complete; not yet run locally/CI |
| Admin product/category edit forms reflect a background refetch on pristine fields while preserving in-progress edits (open form → focus another tab → return) | TASK-141-B | `values`+`keepDirtyValues` shipped; needs running stack to observe refetch behaviour |
| Guest→user cart merge: guest adds items → login/register → items present without reload; reload while logged in keeps cart non-empty (the TASK-118 path) | TASK-118 | `isInitializing` query-gate + post-refresh cart invalidation shipped; needs running stack |
| Checkout submit → lands on `/orders/{uuid}/confirmation` (not empty `/cart`); confirmation page shows UA address (city, delivery address, phone, "Україна") + items/totals; empty-cart and unauth guards still redirect (absorbs TASK-111 §A5) | TASK-119 | redirect-race fix + UA country localization shipped; full checklist in plan 053 |
| Order stock×status matrix: stock drops at creation; forward transitions don't change it; pre-shipment cancel (PENDING/CONFIRMED/PROCESSING→CANCELLED) auto-returns stock to storefront; SHIPPED/DELIVERED cancel + REFUNDED do NOT auto-restock (manual); no double-credit on repeat cancel | TASK-124 | `shouldAutoRestock` guard shipped; 388 backend tests green; run matrix in `manual-qa-master.md` §C2-a on a stack |
| Product card → PDP: clicking a sale card opens with the cheapest/advertised variant pre-selected (not the alphabetically-first); price matches the card; variant override still works. (Thumbnail strip needs TASK-128 multi-image seed to exercise) | TASK-126 | cheapest-variant default shipped; 83 store-client tests green; verify against `manual-qa-master.md` A1 line 207 |
| Nova Poshta live check: with a real `NP_API_KEY` on a running stack — city search returns real settlements, warehouse list populates for a chosen city, checkout shows a real shipping cost + ETA, and the order persists the NP refs + `shippingCost`. (Automated coverage uses a mocked NP client; this validates the live API contract.) | TASK-080-A…D | NP autocomplete + cost/ETA shipped & test-green; needs the API key to verify against the real NP API |
| Admin order detail (B4): on a running stack, the order-status select offers every status and the separate payment-status select changes payment independently — moving status to CONFIRMED does NOT auto-mark PAID, and setting PAID does not change the order status; pre-shipment cancel still auto-restocks | TASK-151 | Automated: 436 store-api unit + 43 order-e2e + 50 store-admin green. Live smoke pending a running stack |
| Admin users filter + ban enforcement (B5): on a running stack, the "Активний/Неактивний/Усі статуси" filter returns the correct set; a banned customer with a live access token gets `403` on `POST /api/orders` (and cannot refresh) | TASK-150 | Automated: 433 store-api + 38 order-e2e + 45 store-admin green. Live smoke pending a running stack. Gap B (other authed endpoints during the ≤15-min token window) deferred by owner decision |
| Tab freeze / bfcache restore: background the tab until the browser freezes it, then return → products/cart/header recover (no infinite skeleton); normal tab switch does not refetch-storm; F5 still works | TASK-120-E | Defensive `useRecoverStrandedQueries` proven via Playwright wedge simulation; real-browser bfcache confirmation still pending (headless Chromium disables true bfcache). NOTE: the originally-reported reopen-browser symptom was a Turbopack dev artifact, absent on prod builds — this row tracks only the separate bfcache wedged-query guard |
| Storefront stock hiding: PDP + cart show only a status (В наявності / Закінчується / Немає), never a raw unit count; public `GET /products` + `/products/:slug` expose no `stock` (only `inStock`/`lowStock`); cart qty stepper still capped per `item.stock` | TASK-132 / TASK-158 | contract + UI shipped; needs a running stack |
| Cart & order line items show the real product image (placeholder fallback) and link to the correct PDP `/products/{slug}`; order-details layout correct on desktop + mobile | TASK-133 / TASK-134 | needs a running stack |
| Header: logged-in user sees the account icon + dropdown (cabinet / orders / logout) and guest sees login/register; mobile Sheet sections; Esc + keyboard nav + focus-return | TASK-130 | needs a running stack |
| Order cancel: a PENDING order shows a Cancel button → confirm flips status to CANCELLED, refetches list/detail, and auto-returns stock (TASK-124 path); non-PENDING shows no button | TASK-131 | needs a running stack |
| Admin: creating a product with an empty slug → backend derives it; live slug preview renders; dead header search removed | TASK-136 | needs a running stack |
| Dashboard shows earned vs unrealized (ordered-but-unpaid) revenue cards; run the unrealized int-spec `npm run test:int -w apps/store-api` against `store_test` | TASK-137 | needs `test:int` DB |
| No Radix `aria-describedby` console warning when opening the mobile-menu Sheet; the "Mail disabled — skipping…" line is visible at info level when mail is disabled; link-preload warning — reproduce in a browser then fix (best-effort; may remain a follow-up) | TASK-138 | needs a running stack / browser |
| Staff preview of deactivated products: deactivate a product → on the admin edit page click "Переглянути" → `/products/preview/{slug}` opens in a new tab showing the full detail + amber "deactivated" banner; the same slug on the storefront `/products/{slug}` still 404s for customers; a non-admin token gets 403 on `GET /api/products/admin/preview/:slug` | TASK-155 | needs a running stack |
| Reviews end-to-end: a logged-in user submits a review on the PDP → it does NOT show until an admin approves it in `/reviews` (moderation queue) → after approve it appears in the PDP Reviews tab with the aggregate rating; verified-purchase badge shows for buyers; duplicate submit returns 409 | TASK-078 / TASK-106 | needs a running stack |
| Variant dots + quick-add: a product card for a grouped product shows colour dots + an advertised "from {price}"; hovering (or keyboard-focusing) the card reveals a quick-add button that adds the default (cheapest) variant to the cart; out-of-stock default disables the button | TASK-077 | needs a running stack |
| Chrome redesign (plan 096): search pill shows [Каталог ▾ | input | 🔍] with the catalog panel + typo-tolerant suggestions both anchored to the pill and mutually exclusive; guest "Кабінет" opens the right auth slide-out (Вхід/Реєстрація tabs, close-on-success, tab-switch links, register terms checkbox, login forgot/Google/Apple stubs toast); header cart button opens a mini-cart slide-out (items + qty stepper + remove reconcile with the cache; total; "Оформити" → /checkout, "Перейти в кошик" → /cart; empty state closes to /products); dark footer renders four columns + social buttons + payment pills; wishlist heart appears on recently-viewed + related cards and toggles | TASK-167-A/B/C | needs a running stack (light + dark mode contrast check on the footer; verify slide-out login/register actually authenticates end-to-end; mini-cart qty/remove reflect on the /cart page) |
| Image optimization: product images load via `next/image` (lazy, with a blur/shimmer placeholder); first above-the-fold row loads eagerly (no layout shift); remote `/uploads/**` host renders without a Next image-host error | TASK-074 | needs a running stack |
| Coupons: admin creates a percent + a fixed code (with min-spend/expiry/caps); in the cart, applying a valid code shows the discount and updated total, an invalid/expired/below-min/used-up code shows the typed error; the order persists `discount`/`discountCode` and increments `redeemedCount`; the server recomputes (a tampered client amount is ignored) | TASK-079 | needs a running stack |
| Wishlist: a guest hearts products (persist across reload via `wishlistToken`); on login/register the guest list merges into the account with no duplicates; the header badge count updates; `/wishlist` lists saved items and remove works | TASK-076 | needs a running stack |
| Mail outbox: placing an order returns immediately and writes a `mail_outbox` PENDING row in the same transaction; with `MAIL_ENABLED=true` the cron worker sends it and marks SENT; a forced SMTP failure retries with backoff then FAILED after maxAttempts; with mail disabled rows drain as no-op SENT | TASK-103 | needs a running stack (SMTP) |
| Image pre-optimization: uploading a JPEG/PNG in admin stores a `.webp` in `/uploads/products/` with a smaller payload than the original and a non-null `blurDataUrl`; the storefront card/PDP shows a real per-image blur-up (not the generic shimmer); an animated GIF still uploads and animates (passthrough, no `blurDataUrl`); existing seed images (no `blurDataUrl`) still render via shimmer — optional one-off backfill is a documented follow-up | TASK-091 | needs a running stack |
| Sentry: with `SENTRY_DSN` / `NEXT_PUBLIC_SENTRY_DSN` set, a deliberate API 500 and a thrown storefront/admin render error both land in the Sentry project tagged with the correct `environment`; a 4xx validation error is NOT sent; with no DSN set all three apps run exactly as before (no-op). CI source-map upload (`SENTRY_ORG`/`PROJECT`/`AUTH_TOKEN`) is a documented follow-up | TASK-048 | needs a DSN + running stack |
| Search UI (browser): the header search box shows a live autocomplete dropdown as you type (debounced), keyboard up/down/enter/esc works, picking a suggestion opens the PDP and Enter opens `/search?q=`; the results page renders product cards + empty state; mobile Sheet hosts the box. (API path already live-verified: typo tolerance + suggest over HTTP against 32 seeded docs.) Requires the `meilisearch` container up (`docker compose up -d meilisearch`) + `MEILI_HOST`/`MEILI_MASTER_KEY` in the API `.env`; without them search transparently falls back to Postgres | TASK-075 | needs a running stack (browser) |
| Blog page (`/blog`) renders pixel-close to the `Blog.dc.html` mockup: hero + search, category chips with counts, featured card in the unfiltered view, responsive grid, empty state, gated load-more, social newsletter block — in light + dark mode; the category filter / search / load-more interactions behave; breadcrumb renders | TASK-167-D | static seed shipped + automated gates green (RTL + typecheck/lint + prod build); needs a running stack for the visual pass. Post cards link to `/blog/{slug}` (404 until TASK-171) and `/blog` is not yet in nav (TASK-173) |

---

## Completed (Phases 1–5)

> One row per parent task. Full sub-task breakdowns: [`docs/backlog-archive.md`](docs/backlog-archive.md).
> Historical duplicate IDs (`TASK-054/058/059` reused; `TASK-051-O` merged) are annotated there.

| Area | Tasks | Status | Plans |
| --- | --- | --- | --- |
| **Phase 1 — Foundation** | TASK-001…005 infra · 006…008 DB · 009…013 backend core (auth/user/product/category) · 014…016 API contract · 017…018 frontend scaffold | ✅ | plans 001–008, 013 |
| **Phase 2 — Storefront & Cart** | TASK-021…027 cart backend · 028…032 storefront pages + add-to-cart · 051 guest cart · 052 storefront auth | ✅ | plans 009–019 |
| **Phase 3 — Checkout & Orders** | TASK-033 order module · 035 checkout · 036 confirmation page · 037 confirmation emails · 053…060 review follow-ups | ✅ | plans 020, 022–024 |
| **Phase 4 — Admin Panel** | TASK-038 RBAC · 039 product mgmt · 040 category mgmt · 041 order mgmt · 042 user mgmt · 043 dashboard · 061…067 review follow-ups | ✅ | plans 025–031 |
| **Phase 5 — Polish & Production** | TASK-044 Redis caching · 045 SEO sitemap/JSON-LD · 046 Helmet/CSRF/rate-limit · 047 Pino logging · 068 UI/UX redesign (core) · 069 UA/UAH localization (storefront) | ✅ | plans 032–035, 039–040 |
| **Tech Debt & Architecture** | TASK-054 monorepo review · 058 orval-config consolidation · 059 baseURL alignment · 071/072 QA bugfixes | ✅ | plans 036–038, manual-qa-master |
| **Product Images** | TASK-073 images (backend + admin **upload** + storefront display) | ✅ | plan 041 |
| **EPIC — Variant-as-Product-Position** | TASK-142 (A–G) — promote every variant to a first-class product position; group siblings via `ProductGroup`; cross-position slug navigation; cart/order/admin/seed/SEO de-varianted | ✅ | plan 060 |

---

## Roadmap (Open) — priority tiers

> Active sequence: **Tier 0 → Tier 1 → Tier 2 → Tier 3 → Tier 4**, then *Parked / Later*.
> Logic that sits behind an already-shipped imported-UI stub is held separately in
> **[Logic behind imported UI](#logic-behind-imported-ui-deferred--do-the-ui-port-first)**
> — deferred by owner decision until the UI port is complete, so it is not part of the tier sequence.
> Each row is a one-liner; full design goes into a `docs/plans/NNN-*.md` when a task is picked up.
> New task IDs use a single monotonic counter (next plain ID **TASK-174**).

### Tier 0 — Critical data & security bugs *(do first — they corrupt real data / leak hidden content)*

| Task ID | Description | Status | Plan |
| --- | --- | --- | --- |
| TASK-143 | **[CRITICAL BUG]** Guest/any add-to-cart persists the item *before* validation → 400 toast but the item is already saved (it appears after a reload). `cart.service.ts:40-55` calls `cartRepository.addItem()` then `validateCartItems()`. Fix: validate stock/`isActive`/max-qty **before** the DB write (Option A — validate-before-write in the service; Option B transaction rollback rejected as violating Clean Architecture). Sub-tasks: ✅ **TASK-143-A** write failing tests (Red), ✅ **TASK-143-B** fix implementation (Green), ✅ **TASK-143-C** refactor + JSDoc. Done: `addToCart` validates the resulting quantity (stock/`isActive`/max) before any write via new `CartRepository.findProductForCartValidation`; `validateCartItems` removed. Verified: 398 unit + 23 cart e2e green, regression guard asserts `addItem` not called on invalid add. | ✅ | [docs/plans/062-cart-add-validate-before-write.md](docs/plans/062-cart-add-validate-before-write.md) |
| TASK-144 | **[CRITICAL BUG]** Stock-0 products can be added from the product **card** button — `product-grid`'s `AddToCartButton` has no stock guard (the PDP `product-detail-view` already uses `disabled={product.stock === 0}`). Add the same guard to the card; pairs with the TASK-143 backend guard. **Audit also found `product-list.tsx` (catalog page) unguarded — both in scope.** Sub-tasks: ✅ **TASK-144-A** add `outOfStock` prop to `AddToCartButton` + dict entry, ✅ **TASK-144-B** apply guard to both widgets, ✅ **TASK-144-C** RTL+MSW tests. Done: stock-0 cards render a disabled "Немає в наявності" button on grid + catalog; `shared/ui/ProductCard` untouched (FSD). Verified: 84 store-client tests green (4 new), lint + typecheck clean. | ✅ | [docs/plans/063-product-card-stock-guard.md](docs/plans/063-product-card-stock-guard.md) |
| TASK-145 | **[SECURITY BUG]** Public PDP serves **deactivated** products by direct slug — `product.repository.ts` `findBySlugWithRelations` filters `deletedAt: null` but not `isActive`. Add `isActive: true` to the public read path (admin/manager preview is TASK-155). Also closes the admin B2 "deactivate still visible on storefront" finding. Sub-tasks: ✅ **TASK-145-A** write failing tests (Red), ✅ **TASK-145-B** fix implementation (Green), ✅ **TASK-145-C** refactor + JSDoc. Done: `findBySlugWithRelations(slug, { activeOnly? })` spreads `isActive: true` into the `where` clause by default (deactivated → 404), leaving `activeOnly: false` open for staff preview (TASK-155); service call site unchanged. Verified: 401 unit tests green (3 new) + new repo/e2e guards, lint + typecheck clean. | ✅ | [docs/plans/064-pdp-deactivated-product-guard.md](docs/plans/064-pdp-deactivated-product-guard.md) |

### Tier 1 — Full Ukrainian localization & UA business *(headline priority)*

> "Localization" here = language **and** Ukrainian business practice: order/payment statuses
> customers understand, hryvnia formatting, `+380` phone, UA addresses, and Nova Poshta delivery.

| Task ID | Description | Status | Plan |
| --- | --- | --- | --- |
| TASK-115 | Finish store-admin UA localization — products/categories/orders/users CRUD (tables, forms, detail views, toggles, toasts, zod validation messages). Expand `apps/store-admin/src/shared/config/dictionary.ts` (~60 hardcoded strings remain). **Done:** shell/nav, header, login, dashboard. | 🔄 | — |
| TASK-129 | User-facing order **&** payment status labels — replace raw enums (`PENDING`…) with adequate UA wording for customers (storefront order history + confirmation) and admin (badge text, status select). Add a shared status→label map per app. | ✅ | [docs/plans/065-order-payment-status-labels.md](docs/plans/065-order-payment-status-labels.md) |
| TASK-148 | **[NEW]** UAH currency localization — admin formatters use `en-US`/`USD`; switch to `uk-UA`/`UAH` in product/order tables, order detail, dashboard. Add UAH formatting to storefront order history/confirmation (amounts currently render without a currency). Sub-tasks: ✅ **TASK-148-A** shared `formatCurrency` helper, ✅ **TASK-148-B** replace `en-US`/`USD` in order/product/detail, ✅ **TASK-148-C** fix `RevenueTrendChart` + consolidate `AdminDashboardStats`, ✅ **TASK-148-D** storefront audit (already done via TASK-069). Done: admin formats all money via shared `apps/store-admin/src/shared/lib/format/formatCurrency.ts` (`uk-UA`/`UAH`); presentation-only (no DB/API). Verified: admin build/lint/typecheck + 19 tests green, store-client 84 tests green; zero `en-US`/`USD` currency formatters remain in admin. | ✅ | [docs/plans/066-uah-currency-localization.md](docs/plans/066-uah-currency-localization.md) |
| TASK-135 | Checkout prefill for logged-in users (pull saved profile/contact data) + UA phone input mask (`+380 …`). Sub-tasks: ✅ **TASK-135-A** `useCheckoutPrefill` hook — fetch profile via `useUserControllerGetProfile`, `reset()` keyed to `user.id` with `keepDirtyValues: true` (forms.md Rule 2b); wire into `CheckoutView`, add `control` prop to `CheckoutAddressForm`. ✅ **TASK-135-B** `PhoneInput` in `shared/ui` — zero-dep controlled component, `formatUAPhone` formats as `+380 NN NNN NNNN`; replace phone field in `CheckoutAddressForm` with `Controller` + `PhoneInput`. ✅ **TASK-135-C** Tests — `PhoneInput` unit tests, updated `checkout-address-form.test.tsx` (supply real `control`), `CheckoutView` prefill integration tests, `makeUser` factory in `msw-handlers.ts`. Note: `city`/`deliveryAddress` have no profile source; prefill covers `firstName`/`lastName`/`phone` only (TASK-080 tracks NP address persistence). Verified: typecheck + lint clean, 103 store-client tests green. | ✅ | [docs/plans/067-checkout-prefill-phone-mask.md](docs/plans/067-checkout-prefill-phone-mask.md) |
| TASK-080 | **[promoted from Phase C]** Delivery + Nova Poshta — city + branch autocomplete via the NP API, persist the chosen branch in the DB, show cost + ETA in checkout. Manual delivery is the interim fallback until this ships. **A–D shipped & automated-test-green (storefront NP autocomplete + cost/ETA on order); E + live NP verification DEFERRED until the real NP API key is provisioned (the `DeliverySetting` migration also needs a running DB) — owner decision 2026-06-27.** Sub-tasks: ✅ **TASK-080-A** backend `DeliveryModule` — `NovaPoshtaClient` (Node.js `fetch`), `DeliveryService` (city/warehouse search + estimate, Redis cache, sender resolves `NP_SENDER_CITY_REF`→Kyiv), `DeliveryController` (`GET /api/delivery/cities`, `/warehouses`, `/estimate`, throttled), env vars `NP_API_KEY` + `NP_SENDER_CITY_REF`. Verified: 9 new unit tests (mock client), 410 store-api tests + build/typecheck/lint green. **Live NP-API check pending the real API key (manual QA).** ✅ **TASK-080-B** `AddressDto` + `ShippingAddressData` extended with optional `npCityRef`/`npWarehouseName`/`npWarehouseRef`; `OrderService` computes `shippingCost` from NP estimate on order create (only when `npCityRef` present; falls back to 0 on error); `OrderRepository.createFromCart` writes cost + `total = subtotal + shipping`; `OrderModule` imports `DeliveryModule`; Orval regen (generated files gitignored). Verified: 413 store-api tests + build/lint/typecheck green; client+admin typecheck against regenerated hooks. ✅ **TASK-080-C** frontend `Combobox` primitive in `shared/ui` (zero-dep, WAI-ARIA, free-text fallback); `NpCityField` + `NpWarehouseField` in `features/checkout/ui` (debounced via direct `useDebouncedCallback`, forms.md Rule 3, sets `npCityRef`/`npWarehouseRef` + display text); `entities/delivery` barrel; replaced free-text city/deliveryAddress with NP autocompletes; live shipping cost + ETA in `CheckoutOrderSummary` (gated on `npCityRef`); schema/use-checkout/prefill carry NP refs; dict strings added. Verified: 103 store-client tests + build/lint/typecheck green (existing checkout tests updated + delivery MSW handlers added). ✅ **TASK-080-D** tests — `DeliveryService` unit (9, mock client, TASK-080-A), `delivery.e2e-spec` (6, mocked Prisma + NP client → controller/validation/mapping, no DB/key), `Combobox` RTL (8), `NpCityField` RTL+MSW (4), updated `checkout-address-form.test.tsx` + delivery MSW handlers. Verified: 115 store-client + delivery e2e green. 🅿️ **TASK-080-E _(DEFERRED until NP API key + running DB)_** admin-configurable dispatch origin — `DeliverySetting` singleton (Prisma migration: sender city ref/name + optional sender warehouse + default weight), `DeliveryRepository`, admin-only `GET`/`PUT /api/admin/delivery-settings`, store-admin settings page (NP city picker); estimate resolves origin/weight as `DeliverySetting` → `NP_SENDER_CITY_REF` → Kyiv default. Internal data, never shown to customers. | 🅿️ | [docs/plans/068-nova-poshta-delivery.md](docs/plans/068-nova-poshta-delivery.md) |

### Tier 2 — Critical functional bugs *(broken flows, not data-corrupting)*

| Task ID | Description | Status | Plan |
| --- | --- | --- | --- |
| TASK-146 | **[NEW BUG]** Checkout steps "Доставка / Перевірка / Підтвердження" are cosmetic — `checkout-view.tsx:106` hardcodes `CheckoutStepIndicator current={1}` and the single form submits the order directly. Implementing the real multi-step flow (Option A, recommended): Delivery → Review → Confirm. Single `useForm` retained; `step` state (`1\|2`) drives the indicator; "Далі" runs `form.trigger(deliveryFields)` before advancing; order created only on the final step-2 submit. No backend/API/Orval changes. Sub-tasks: ✅ **TASK-146-A** `useCheckoutSteps` hook (step state + `trigger`-gated `goToReview`/`goToDelivery`, `isValidating` guard) + indicator wired to live `step` + Next/Back nav (`type="button"`) + focus management + dict keys (`nextStep`/`prevStep`/`reviewHeading`), ✅ **TASK-146-B** `CheckoutReviewStep` read-only `useWatch` summary (forwarded focusable heading), ✅ **TASK-146-C** tests — updated 2 existing checkout-view tests + 5 new step-flow tests. Frontend-only; order created only on the step-2 submit. Verified: 120 store-client tests + build/lint/typecheck green. | ✅ | [docs/plans/069-checkout-multi-step-flow.md](docs/plans/069-checkout-multi-step-flow.md) |
| TASK-147 | **[NEW]** Admin tables column sorting (products / orders / users) — clickable sortable headers + `sortBy`/`sortOrder` URL params. Owner flagged **critical**. Backend gap (orders + users hardcoded `createdAt desc`) closed. Sub-tasks: ✅ **TASK-147-A** backend sort for orders (`AdminOrderListQueryDto` `createdAt`/`total`/`status` + `order.repository.ts` allow-list + `@ApiQuery`) and users (`UserListQueryDto` `createdAt`/`email` + `user.repository.ts`/`user.service.ts` + `@ApiQuery`) + Orval regen, ✅ **TASK-147-B** shared `SortableColumnHeader` (`shared/ui/`, WAI-ARIA `aria-sort` + chevron) + `useTableSort` hook (`shared/lib/`, URL-synced, resets page) + dict `common.sort*` keys, ✅ **TASK-147-C** wired all three tables (products Name/Price/Created — hardcoded sort removed; orders Created/Total/Status; users Email/Joined), all standardised to `router.replace`, ✅ **TASK-147-D** tests — `SortableColumnHeader` RTL (5), `useTableSort` unit (5), extended `admin-order-table.test.tsx` + new product/user table tests, backend repo sort specs. Verified: 419 store-api + 34 store-admin tests + build/lint/typecheck green. Note: `status` sort is enum-alphabetical in Postgres (MVP). | ✅ | [docs/plans/070-admin-table-sorting.md](docs/plans/070-admin-table-sorting.md) |
| TASK-149 | **[NEW BUG]** Admin category parent selection is not persisted — choosing a parent on create/edit resets in the UI on save (B3). Root cause: `category-form.tsx` used RHF `values`+`keepDirtyValues` (Rule 2a) which clobbered the in-progress parent before its dirty flag committed; the update path also mapped blank parent → `undefined` (Prisma "no-change") so an existing parent could never be cleared. Sub-tasks: ✅ **TASK-149-A** backend — `UpdateCategoryDto.parentId` accepts `string \| null` (`@ValidateIf` skips UUID check on null + `nullable`/`type: String` for clean Orval output) so "make root" is expressible; service/repo already handled null; added "clear parent" service spec + Orval regen. ✅ **TASK-149-B** `CategoryForm` migrated to forms.md Rule 2b — dropped `values`/`resetOptions`, added `id` prop + `useEffect(reset, [id])` (re-seeds only on entity navigation, never on a background refetch); `edit-category-view.tsx` passes `id`. ✅ **TASK-149-C** `categoryFormValuesToDto(values, { isUpdate })` overload — blank parent → `null` on update (explicit clear), `undefined` on create; edit view passes `{ isUpdate: true }`. ✅ **TASK-149-D** tests — new `category-form.test.tsx` (8: create selection→submit, Rule 2b reset-keyed-to-id via the `name` field, same-id no-clobber via a real selection, `categoryFormValuesToDto` null mapping) + backend "clear parent" spec. Note: the Rule 2b reset is asserted through the `name` field because Radix's BubbleSelect drops a programmatically-reset value under jsdom (jsdom-only artifact — real browsers don't fire `change` on a programmatic value set). Display Layer 3 (`admin-category-table.tsx` in-page `nameById`) left as a documented follow-up. Verified: 420 store-api + 42 store-admin tests + typecheck/lint/build green. | ✅ | [docs/plans/071-admin-category-parent-persistence.md](docs/plans/071-admin-category-parent-persistence.md) |
| TASK-150 | **[NEW BUG]** Admin users — "All statuses" filter doesn't filter (B5); banned/inactive customer is still able to order and isn't session-revoked. **B5 root cause:** the global `ValidationPipe` (`enableImplicitConversion`) coerces `isActive` to `Boolean(value)` *before* the hand-rolled `@Transform` runs, and `Boolean('false') === true`, so every selection collapsed to `undefined` (no filter). The planned "remove `@Transform`" fix was proven wrong (would make `false`→`true`); instead `@Transform` now reads the raw `obj[key]` query string. **Ban gap:** `createOrder` had no active-user guard — a banned user with a still-valid access token could place orders (refresh tokens were already revoked on ban). Sub-tasks: ✅ **TASK-150-A** `@Transform` reads `obj[key]`, ✅ **TASK-150-B** backend tests (new `user-list-query.dto.spec` reproduces B5 under implicit conversion + repo `isActive` where-clause + service forwarding), ✅ **TASK-150-C** `ForbiddenException` guard at top of `createOrder` (inactive/missing user; fetched user reused for the confirmation email — single fetch), ✅ **TASK-150-D** TDD order ban-guard specs (403 + guard-precedes-cart + single-fetch + e2e 403), ✅ **TASK-150-E** `AdminUserTable` status-filter URL tests (active→`isActive=true`, inactive→`false`, all→cleared). Also fixed pre-existing `order.e2e-spec` drift (missing `findByIdForAdmin` mock from TASK-125; stale 2-arg `updateStatus` assertion from TASK-123). No DB migration — uses existing `isActive`. **Deferred (owner decision):** Gap B — `JwtAccessStrategy` still trusts the token payload without a per-request DB check, so ban is immediate on order creation but other authed endpoints stay open for the ≤15-min access-token window. Verified: 433 store-api unit + 38 order-e2e + 45 store-admin + typecheck/lint/build green. | ✅ | [docs/plans/072-admin-user-status-filter-and-ban-enforcement.md](docs/plans/072-admin-user-status-filter-and-ban-enforcement.md) |
| TASK-151 | **[NEW]** Admin order management rework — admin now has **full manual control** over order status and a **separate payment-status control** decoupled from the `PENDING→…→DELIVERED` pipeline (owner's B4 remark). Sub-tasks: ✅ **TASK-151-A** TDD Red — decoupled `updateStatus` + `adminUpdatePaymentStatus` specs, ✅ **TASK-151-B** TDD Green — `derivePaymentStatus` deleted (`updateStatus` forwards current `paymentStatus` unchanged), `adminUpdatePaymentStatus` added, TASK-123 coupling block replaced with "preserved unchanged" assertions, ✅ **TASK-151-C** `UpdateOrderPaymentStatusDto` + `PATCH /api/admin/orders/:id/payment-status` + `confirmPayment`/`markPaid`/`confirm-payment` marked `@deprecated`, ✅ **TASK-151-D** Orval regen offline (`swagger:export`→`generate:api`; new `useAdminOrderControllerUpdatePaymentStatus` hook + `UpdateOrderPaymentStatusDto` model), ✅ **TASK-151-E** frontend — status select unrestricted (all statuses **except current**) + new `PaymentStatusSelect` feature slice + UA dict keys, wired both into `order-detail-view`, ✅ **TASK-151-F** RTL for `PaymentStatusSelect` (4) + extended order-detail view test. TASK-124 auto-restock invariant preserved (untouched); 5 new payment-status e2e cases added. No Prisma migration. **Legacy removal** (`confirmPayment`/`markPaid`/`confirm-payment` route) tracked as a follow-up. Verified: 436 store-api unit + 43 order-e2e + 50 store-admin + typecheck/lint/build green. | ✅ | [docs/plans/073-admin-order-management-rework.md](docs/plans/073-admin-order-management-rework.md) |
| TASK-152 | **[NEW BUG]** Dashboard "Top products" / "Low-stock" panels are not visible / show empty (B6). **Root cause was twofold:** (1) the "Top products" widget **never existed** — `dashboard-view` only mounted stats/charts/low-stock; (2) `getTopProducts()` + the revenue aggregates filtered `status NOT IN (CANCELLED, REFUNDED)`, a paid-proxy that TASK-151 invalidated (status no longer implies paid). Sub-tasks: ✅ **TASK-152-A** rewrote stale int-spec to the current schema (no `ProductVariant`/`variantId`; `paymentStatus`-seeded orders), ✅ **TASK-152-B** `getTopProducts` + `getTotalRevenue`/`getRevenueSince`/`getRevenueByDay` now filter `payment_status = 'PAID'` (earned revenue; `NON_REVENUE_STATUSES`/`OrderStatus` import dropped), ✅ **TASK-152-C** fixed stale `dashboard.e2e-spec` fixture/assertions (`lowStockVariants`→`lowStockProducts`) + repo JSDoc, ✅ **TASK-152-D** new `DashboardTopProductsTable` widget (rank / name / UAH revenue, empty-state, dict keys), ✅ **TASK-152-E** wired into `dashboard-view` (between charts and low-stock) + barrel export + RTL tests. No Orval regen, no migration. Complements TASK-137 (broader unrealized-revenue audit builds on this "earned = PAID" ground truth). Verified: 436 store-api unit + 4 dashboard-e2e + 52 store-admin + typecheck/lint/build green. **Int-spec run pending a DB** (see Pending manual QA). | ✅ | [docs/plans/074-dashboard-top-products-low-stock.md](docs/plans/074-dashboard-top-products-low-stock.md) |

### Tier 3 — UX, data & admin polish

| Task ID | Description | Status | Plan |
| --- | --- | --- | --- |
| TASK-127 | Loading states / skeletons across storefront + admin — give feedback on slow actions ("немає лоадерів"). **Audit complete; plan written; all sub-tasks implemented & verified (build/lint/typecheck green, 52/52 admin tests pass).** Sub-tasks: ✅ **TASK-127-A** admin shared `Skeleton` primitive + `AdminFormSkeleton` + `AdminProductGroupTableSkeleton` + fix 6 bare `<Suspense>` fallbacks (products/new, categories/new, categories/[id]/edit, product-groups page/new/[id]/edit), ✅ **TASK-127-B** storefront `loading.tsx` files for 7 route segments (products/, products/[slug]/, cart/, checkout/, orders/, orders/[id]/confirmation/, account/) + `AccountSkeleton` widget, ✅ **TASK-127-C** admin `loading.tsx` files for 13 route segments (dashboard + all major sub-routes + product-groups) — depends on TASK-127-A and TASK-127-E (Sub-B), ✅ **TASK-127-D** admin table `isFetching` overlay for `AdminProductTable`/`AdminOrderTable`/`AdminUserTable` during sort/filter/pagination refetches, ✅ **TASK-127-E** storefront `ProductList` `isFetching` overlay (Sub-A) + admin `DashboardSectionSkeleton` for charts/top-products/low-stock panels (Sub-B). | ✅ | [docs/plans/075-loading-states-skeletons.md](docs/plans/075-loading-states-skeletons.md) |
| TASK-130 | Header account → user icon + dropdown; ensure the customer cabinet link is visible | ✅ | [docs/plans/077-header-account-dropdown.md](docs/plans/077-header-account-dropdown.md) |
| TASK-131 | Storefront user order cancellation — cancel button for PENDING orders (backend cancel already exists) | ✅ | [docs/plans/078-storefront-order-cancel.md](docs/plans/078-storefront-order-cancel.md) |
| TASK-158 | **[PREP — Wave 0]** Line-item product link contract + public stock hiding — backend-only prep that unblocks TASK-132/133/134 Wave-2 frontend branches. (1) Add `productSlug` + `imageUrl` to `CartItemEntity`/`OrderItemEntity`; extend `CART_ITEMS_INCLUDE`/`ORDERS_INCLUDE` Prisma selects. (2) Introduce `PublicProductEntity` (`inStock`/`lowStock`, no raw `stock`) for public `GET /products` + `GET /products/:slug`; extract `LOW_STOCK_THRESHOLD` to `product/product.constants.ts`. (3) Orval regen. Branch: `feat/132-line-item-contract`. Sub-tasks: ✅ **TASK-158-A** TDD Red — entity mapper specs (CartItem + OrderItem slug/image), ✅ **TASK-158-B** TDD Green — implement entities + repo selects, ✅ **TASK-158-C** TDD Red — PublicProductEntity specs (inStock/lowStock/no-stock), ✅ **TASK-158-D** TDD Green — implement PublicProductEntity + constant + service + controller, ✅ **TASK-158-E** TDD Refactor — JSDoc + full suite green, ✅ **TASK-158-F** Orval regen + contract verification. | ✅ | [docs/plans/076-line-item-contract.md](docs/plans/076-line-item-contract.md) |
| TASK-132 | Hide raw stock quantity from customers on the storefront — **Wave 2 (frontend only); blocked by TASK-158 prep merging first.** Storefront renders `inStock`/`lowStock` from the new `PublicProductEntity` fields; `product-stock-indicator.tsx` shows status badge (В наявності / Закінчується / Немає), not the number. Sub-tasks: ✅ **TASK-132-A** dictionary (`lowStock` → static string "Закінчується"; dead `inStock(n)` removed), ✅ **TASK-132-B** rewrite `ProductStockIndicator` with `{ inStock, lowStock }` boolean props, ✅ **TASK-132-C** update PDP view call sites (`<ProductStockIndicator>` + two `disabled` guards), ✅ **TASK-132-D** update card guard sites in `product-list.tsx` + `product-grid.tsx`, ✅ **TASK-132-E** update `buildProductSchema.ts` type + `product.inStock`, ✅ **TASK-132-F** new `product-stock-indicator.test.tsx` (3 cases, asserts no raw number) + updated fixtures in `product-detail-view.test.tsx` and `schema.test.ts`. `CartItemEntity.stock` is retained — cart uses it for qty-cap logic only, never displays it. | ✅ | [docs/plans/079-hide-stock-storefront.md](docs/plans/079-hide-stock-storefront.md) |
| TASK-133 | Cart line images + product links — replace `ProductThumb` placeholder with the real image and link to the PDP — **Wave 2 (frontend only); blocked by TASK-158 prep merging first.** Uses `imageUrl` + `productSlug` from the updated `CartItemEntity`. | ✅ | [docs/plans/080-cart-line-images-links.md](docs/plans/080-cart-line-images-links.md) |
| TASK-134 | Order-details page — fix layout + link items to their products — **Wave 2 (frontend only); blocked by TASK-158 prep merging first; must land after TASK-131.** Uses `imageUrl` + `productSlug` from the updated `OrderItemEntity`. Extracts `OrderItemRow` (`"use client"`) from `order-item-list.tsx`; adds image thumbnail + PDP link (bare `<img>` + `onError` + `ProductThumb` fallback, identical to TASK-133 cart pattern); fixes `order-confirmation-view.tsx` layout (`<section>` → `<div>` for content column; CTA strip moved inside content column so buttons sit under items/address on desktop). | ✅ | [docs/plans/081-order-details-layout-links.md](docs/plans/081-order-details-layout-links.md) |
| TASK-136 | Admin — generate slug on the fly on product create; remove or implement the dead header search | ✅ | [docs/plans/082-admin-slug-and-dead-search.md](docs/plans/082-admin-slug-and-dead-search.md) |
| TASK-137 | Admin revenue calc audit — count only earned revenue; show unrealized-but-ordered separately | ✅ | [docs/plans/083-revenue-audit-unrealized.md](docs/plans/083-revenue-audit-unrealized.md) |
| TASK-138 | a11y / console-warning cleanup — `DialogContent` missing `aria-describedby` (sheet.tsx), link-preload warning; add the "Mail disabled — skipping…" log line | ✅ | [docs/plans/084-a11y-console-cleanup.md](docs/plans/084-a11y-console-cleanup.md) |
| TASK-153 | **[NEW]** Admin-managed static/service pages (Політика конфіденційності, Часті питання, Повернення та обмін, …) via a rich-text editor + dynamic storefront routes. **Editor: Tiptap (HTML storage). Storefront URL: `/info/[slug]`.** Sub-tasks: ✅ **TASK-153-A** Prisma `Page` model + migration `add_page_model`, ✅ **TASK-153-B** `PageRepository` (TDD Red→Green), ✅ **TASK-153-C** `PageService` (TDD Red→Green), ✅ **TASK-153-D** controllers + DTOs + entities + module registration, ✅ **TASK-153-E** Orval regen (store-admin + store-client), ✅ **TASK-153-F** `RichTextEditor` shared/ui component in store-admin (reusable for product descriptions), ✅ **TASK-153-G** Admin pages management UI (FSD: entity + feature + widgets), ✅ **TASK-153-H** Admin app routes (`/pages`, `/pages/new`, `/pages/[id]/edit`) + sidebar nav entry, ✅ **TASK-153-I** Storefront `/info/[slug]` SSR route + DOMPurify sanitization + sitemap extension, ✅ **TASK-153-J** Tests — repo+service unit (28), form-mapper + admin RTL (9), pages e2e (13) all green. | ✅ | [docs/plans/085-admin-static-pages-rich-text.md](docs/plans/085-admin-static-pages-rich-text.md) |
| TASK-154 | **[NEW]** Admin-managed site contact block — singleton `SiteContactSettings` row (email, phone, working hours, social links) managed via the admin panel and displayed in the storefront footer. **Singleton pattern:** fixed well-known `SINGLETON_ID` + upsert-on-write; `GET` always returns data (null fields when not yet set). Social links (Viber/Telegram/Instagram) included now so TASK-089 needs no new migration. Sub-tasks: ✅ **TASK-154-A** Prisma `SiteContactSettings` model + migration `add_site_contact_settings`, ✅ **TASK-154-B** `SiteContactRepository` (TDD Red→Green), ✅ **TASK-154-C** `SiteContactService` (TDD Red→Green), ✅ **TASK-154-D** controllers + DTO + entity + `SiteContactModule` registration, ✅ **TASK-154-E** Orval regen (store-admin + store-client), ✅ **TASK-154-F** seed default contact row, ✅ **TASK-154-G** Admin UI (FSD: entity + feature + widget + route `/settings/contact` + nav item), ✅ **TASK-154-H** storefront footer integration (async server component + ISR 1h), ✅ **TASK-154-I** tests — repo/service unit + e2e + admin RTL. | ✅ | [docs/plans/086-admin-site-contact-settings.md](docs/plans/086-admin-site-contact-settings.md) |
| TASK-155 | **[NEW]** Admin/manager-only **preview of deactivated products** — the allowed path behind the TASK-145 public block (customers blocked, staff can preview hidden products live). New `GET /api/products/admin/preview/:slug` endpoint (AdminGuard) + store-admin preview page + "Preview" link on the edit page. Sub-tasks: ✅ **TASK-155-A** TDD Red — 4 service-spec + 4 e2e failing tests. ✅ **TASK-155-B** TDD Green — `ProductService.findBySlugForAdminPreview` calls `findBySlugWithRelations(slug, { activeOnly: false })` (the TASK-145-reserved bypass), skips the public cache, throws 404 on null; `GET admin/preview/:slug` declared **before** `admin/:id` (route-collision guard) with `AdminGuard` + `AdminProductPreviewResponseEnvelope` Swagger model + stable `operationId`. ✅ **TASK-155-C** TDD Refactor — JSDoc (cache-skip rationale, declaration-order note). ✅ **TASK-155-D** Orval regen → `useProductControllerPreviewProductBySlug` hook. ✅ **TASK-155-E** `AdminProductPreviewView` widget (deactivated banner, image strip, price, meta, attributes, plain-text description (product desc is a textarea, not HTML — no DOMPurify needed), active-only group siblings) + 3 RTL tests + 18 dict keys. ✅ **TASK-155-F** `products/preview/[slug]` route + `loading.tsx` (not in sidebar). ✅ **TASK-155-G** "Переглянути" link on `EditProductView` (new tab, slug from cached product). Done: customers stay blocked (TASK-145 regression test still green); only ADMIN can preview (401/403/404 covered). Verified: 482 store-api unit + 28 product-e2e + 77 store-admin (3 new) + typecheck/lint/build green across all workspaces. | ✅ | [docs/plans/087-admin-staff-preview-deactivated-products.md](docs/plans/087-admin-staff-preview-deactivated-products.md) |
| TASK-156 | **[NEW]** Admin order-detail line items → link to the product edit page (admin side of the cart/order product-link work; storefront side is TASK-133/134). | ✅ | [docs/plans/088-admin-order-line-item-product-links.md](docs/plans/088-admin-order-line-item-product-links.md) |
| TASK-162 | **[NEW — storefront design]** Homepage redesign from the Claude Design import (`Homepage.dc.html`). Reworked the **existing** `store-client` homepage in place (no new pages/dups), decomposed per FSD: page composes widgets; base components reused from `@/shared/ui`. Sections: reworked **hero-banner** widget (client `HeroSlider` 3-slide carousel + autoplay, `HeroCategorySidebar` from real category data, `ModelPicker` **UI-only stub** — API has no device→accessory filter, submit no-ops, TASK-165, `PromoTiles`), reworked **TrustStrip** (4 icon cards), reworked **category-nav** into showcase cards (no invented product counts — `CategoryEntity` has no count; images need `Category.image`, TASK-083), reworked **product-grid** → **PopularRail** tabbed horizontal rail (Новинки=`createdAt desc`, Акційні=client-filter `compareAtPrice>price`, **Хіти=best-effort default listing**, needs bestseller signal TASK-164), new **promo-banner** / **newsletter** (social links stub `href="#"`, TASK-166) / **recently-viewed** (read-only `useSyncExternalStore` over localStorage; hidden while empty; write side ready but unwired, TASK-163). All copy centralized in `dict.home`. Verified: store-client lint + typecheck + **182 tests** green, prod build green (`/` static). | ✅ | — |
| TASK-163 | **[NEW — follow-up of TASK-162]** Wire the recently-viewed **write side** on the product-detail page — `ProductDetailView` now calls `pushRecentlyViewed(...)` in an effect keyed to `product.id` when a PDP renders, so the homepage "Ви переглядали" section populates. Delivers the remainder of parked TASK-087. store-client lint/typecheck + product-detail/product-grid tests green. | ✅ | — |
| TASK-167-A | **[NEW — TASK-167 chrome slice]** Header redesign to the design import — static announcement bar (message + phone), logo mark treatment, **Каталог** dropdown on **real root categories** (root-category portion of TASK-082), search block wiring (reuse `SearchAutocomplete`), restyled action cluster (Акції / Обране / Кабінет / Кошик + cart total), mobile Sheet. Also delivers the phone portion of TASK-089. New `announcement-bar.tsx`; `HeaderCartBadge` → primary button with subtotal; `HeaderWishlistBadge` labelled; mobile Sheet lists categories. **Search reworked to the design's single pill** — new `header-search.tsx` merges the "Каталог" mega-menu trigger (left segment, real root categories) + input + primary submit, with both dropdowns anchored to the pill (replaces the standalone catalog button + `SearchAutocomplete` on desktop; mobile Sheet keeps `SearchAutocomplete`). **Guest "Кабінет" now opens a right auth slide-out** (`features/auth/AuthSheet`) with Вхід/Реєстрація tabs reusing the existing `LoginForm`/`RegisterForm` in slide-out mode (close-on-success, tab-switch instead of navigate); register adds a required terms-consent checkbox; login adds mockup extras — "Забули пароль?" + Google/Apple buttons are **stubs** (toast) pending TASK-168/169. Auth forms/sheet polished (cursor-pointer, hover states, tab fade + press animations). **Header cart button now opens a mini-cart slide-out** (`widgets/cart/CartSheet`) — reuses the cached `useGetCart` + `CartItemRow` (optimistic qty/remove), footer total + "Оформити"/"Перейти в кошик", empty/loading/error states; the full `/cart` page stays reachable. **Slide-out animations enabled** — the shadcn `Sheet`/`Dialog` were scaffolded expecting the `animate-in`/`slide-in-from-*` utilities but no animation lib was installed (they were dead classes); added **`tw-animate-css`** (`@import` in `globals.css`), so the auth + cart + mobile slide-outs now slide in with a fading backdrop (and the hero/tab fades go live too). store-client lint/typecheck + 182 tests + prod build green. | ✅ | [docs/plans/096-storefront-header-footer-card.md](docs/plans/096-storefront-header-footer-card.md) |
| TASK-167-B | **[NEW — TASK-167 chrome slice]** Footer redesign — dark re-theme (`bg-foreground`), restructured columns (Brand+socials / Каталог / Інформація with `/info/[slug]` links / Контакти), social square-buttons + contact from `SiteContactSettings`, payment **text-pills**, kept/restyled trust strip. Delivers the footer + contact/social slice of TASK-089. Kept the ISR `SiteContactSettings` fetch + `dict.footer` fallbacks. Info-column links are storefront-safe placeholders (→ `/products`) until `/info/[slug]` slugs are wired (TASK-153/166). store-client lint/typecheck/tests + prod build green. | ✅ | [docs/plans/096-storefront-header-footer-card.md](docs/plans/096-storefront-header-footer-card.md) |
| TASK-167-C | **[NEW — TASK-167 chrome slice]** Product-card audit — wishlist-heart consistency across all card render sites (PopularRail ✅, product-list ✅, product-related, **recently-viewed** needs a heart) + minor token/spacing/focus polish. No quick-view/compare (TASK-086/085 stay parked). Added `WishlistToggleButton` to `product-related` and to the `recently-viewed` card (as a sibling of the link, not nested in the anchor). **Card footer reworked to the exact mockup** (`_action(p)`): a new `ProductCardActions` widget renders an availability line (`● В наявності · доставка 1–2 дні`) + a filled **«Купити»** primary button (not "Додати до кошика", no cart icon) next to an **icon-only outline wishlist heart** — the heart moved off the image into the footer, and the hover-reveal quick-add is gone. Applied across all four listing callers (PopularRail / product-list / product-related / search-results). `AddToCartButton compact` → primary "Купити". Plus a smooth **image hover-zoom** on `ProductCard`. **Notifications unified to the mockup toast:** the single sonner `<Toaster>` restyled to **bottom-centre, dark** (foreground surface / background text, inverted per theme) with coloured status icons (dropped the `top-center`/`richColors` override); all `toast()` calls (add-to-cart, wishlist, auth) share it. store-client lint/typecheck + 182 tests + prod build green. | ✅ | [docs/plans/096-storefront-header-footer-card.md](docs/plans/096-storefront-header-footer-card.md) |
| TASK-167-D | **[NEW — TASK-167 page slice]** Blog listing page (`/blog`) from the Claude Design `Blog.dc.html` import — new `app/blog/page.tsx` composes a new **`widgets/blog`** (FSD): a client `BlogView` (hero + search, category chips with live counts, featured hero card in the unfiltered view, responsive `auto-fill minmax(304px)` grid, empty state, gated «Показати більше статей», social newsletter block) over a **static seed** `widgets/blog/model/posts.ts` (12 posts, token-derived oklch cover gradients) — pixel-mapped to the mockup; placeholder brand "volta" localized → "MobileStore"; inline SVG glyphs copied from the mockup (lucide dropped brand marks); copy centralized in `dict.blog` + `dict.meta.blog*`; breadcrumb JSON-LD; reuses the shared `Header`/`Footer` chrome. **No backend yet** — filter/search/pagination run client-side; post cards link to `/blog/{slug}` which 404s until the Article page (TASK-171) ships, and `/blog` is not yet in header/footer nav (TASK-173). Verified: new `blog-view` RTL (4 cases) + store-client typecheck/lint + prod build green (`/blog` prerendered static). | ✅ | — |
| TASK-167 | **[NEW — storefront design]** Roll the Claude Design system out to the **remaining** imported pages (Catalog, Product, Cart, Checkout, Account, Wishlist, Categories, Blog/Article, Legal, Contact, NotFound) the same way TASK-162 did the homepage — rework existing routes in place, FSD decomposition, reuse `@/shared/ui`. Also align the shared **SiteHeader** (search + catalog mega-menu, parked TASK-082) and **SiteFooter** with the design. Break into per-page sub-tasks when picked up. **Shared-chrome slice (header/footer/card) planned in [docs/plans/096](docs/plans/096-storefront-header-footer-card.md) → TASK-167-A/B/C.** **Blog listing page shipped as TASK-167-D (static seed); its missing backend/article/admin logic is split into TASK-170–173.** | ⬜ | [docs/plans/096-storefront-header-footer-card.md](docs/plans/096-storefront-header-footer-card.md) |

### Tier 4 — Commerce, discovery & reliability *(features)*

| Task ID | Description | Status | Plan |
| --- | --- | --- | --- |
| TASK-078 | Product reviews — write flow + moderation (auth'd submission, verified-purchase, PDP list, admin approval queue) | ✅ | [docs/plans/089-product-reviews.md](docs/plans/089-product-reviews.md) |
| TASK-106 | Reviews module backend — controller/service/repository over existing `Review` model (prereq for TASK-078) | ✅ | [docs/plans/089-product-reviews.md](docs/plans/089-product-reviews.md) |
| TASK-079 | Coupons / promo codes — `Discount` model (percent/fixed, min-spend, expiry, usage caps), apply in cart/checkout, admin CRUD | ✅ | [docs/plans/090-coupons-discounts.md](docs/plans/090-coupons-discounts.md) |
| TASK-075 | Full-text search + header autocomplete — Meilisearch (typo-tolerant) behind `/search`; inline header dropdown. **[Wave 4 ✅]** `SearchModule`: `MeiliClient` wrapper (SDK, `isConfigured()` gate, errors never 500 the storefront) → `SearchService` (index settings: searchable name/description/categoryName, typo tolerance, `reindexAll`) → public `GET /api/search` (Meili rank → Postgres hydration for full `ProductCard`) + `/api/search/suggest` (doc fields, no 2nd DB hit) + admin `POST /api/admin/search/reindex`; **graceful Postgres-`contains` fallback** when the engine is unconfigured. Index sync wired into product `create/update/activate` (upsert) + `deactivate/delete` (remove), best-effort (never blocks the write); bootstrap reindex on init. Storefront `SearchAutocomplete` in the header (desktop + mobile) + `/search?q=` results page. Infra: `meilisearch` v1.10 in docker-compose. **Live-verified end-to-end over HTTP** against 32 seeded docs: typo `ihpone`→iPhone (10), `samsng`→Samsung (4), suggest works. No migration. Verified: store-api 625 unit + search-e2e (mocked) / full e2e 19 suites 257; store-client 182; build/lint/typecheck green; Orval regen clean. | ✅ | [docs/plans/095-meilisearch-search.md](docs/plans/095-meilisearch-search.md) |
| TASK-076 | Wishlist / favorites — guest-via-cookie + merge-on-login (mirrors guest-cart pattern) | ✅ | [docs/plans/091-wishlist-favorites.md](docs/plans/091-wishlist-favorites.md) |
| TASK-077 | Variant dots + quick-add — surface variant summary on list API; color dots + hover ATC overlay | ✅ | — |
| TASK-074 | Image optimization — `next/image` remotePatterns + shimmer placeholder | ✅ | docs/plans/042-image-optimization.md |
| TASK-091 | Origin-side image pre-optimization — `sharp` WebP renditions + per-image LQIP `blurDataUrl` on upload. **[Wave 3 / Stream A ✅]** New injectable `ImageProcessor` (`sharp`) converts uploads to WebP + generates a ~16px WebP LQIP `data:` URI; `image/gif` passes through unconverted (`blurDataUrl` null). Migration `add_image_blur_data_url` (nullable) landed schema-first; `blurDataUrl` persisted + exposed on admin entity **and** public `GET /products`/`:slug`; storefront `ProductCardImage` + PDP gallery render `placeholder="blur"` when present, else the TASK-074 shimmer. Verified: store-api 588 unit + product-images e2e 12/12; store-client 172 (ProductCardImage blur/shimmer); build/lint/typecheck green; Orval regen clean. | ✅ | [docs/plans/093-image-preoptimization-sharp.md](docs/plans/093-image-preoptimization-sharp.md) |
| TASK-048 | Sentry integration (frontend + backend) — `@sentry/nestjs` + `@sentry/nextjs`, wire to Pino error path. **[Wave 3 / Stream B ✅]** Backend `instrument.ts` (imported first in `main.ts`) + `SentryModule.forRoot()`; `HttpExceptionFilter` forwards only 5xx/unhandled to `captureException` (4xx skipped), envelope unchanged; optional `SENTRY_DSN`/`SENTRY_ENVIRONMENT`/`SENTRY_TRACES_SAMPLE_RATE`. Frontends (Next 16.2.4 + `@sentry/nextjs` 10.62.0, modern `instrumentation.ts`/`instrumentation-client.ts` layout, `global-error.tsx`, `withSentryConfig`). **DSN-gated no-op** — all 3 apps build/boot/test unchanged without a DSN. Verified: store-api 588 unit (new filter + env specs); build/lint/typecheck green all workspaces. | ✅ | [docs/plans/094-sentry-integration.md](docs/plans/094-sentry-integration.md) |
| TASK-103 | Mail reliability — replace fire-and-forget with transactional outbox + retry worker | ✅ | [docs/plans/092-mail-outbox.md](docs/plans/092-mail-outbox.md) |
| TASK-104-J | Soft deletes — integration + **live DELETE smoke done**. Migration `add_soft_delete_audit` applied (`prisma migrate status` → up to date). Verified on the running stack: `DELETE /api/products/:id` → 204, product gone from public detail (404) + listing (total 0), DB row persists with `name` intact + slug mangled `deleted:<id>:<slug>` + `is_active=false` + tombstoned (order-history name resolves); `DELETE /api/users/:id` → 204, gone from admin list, email mangled with `original_email` preserved, **re-registration of the same email → 201** (unique slot freed). Throwaway rows cleaned up. | ✅ | docs/plans/047-soft-deletes-audit.md |
| TASK-105 / 105-D | Frontend test harness — RTL/MSW + store-admin Jest shipped; **pending:** run Playwright E2E (DB + browsers) | 🔄 | docs/plans/048-frontend-test-harness.md |
| TASK-101 | Run the *Pending manual QA* list to closure (Redis int, dashboard int, JSON-LD live, admin CSRF smoke) | ⬜ | — |
| TASK-157 | **[NEW]** Remove the deprecated payment-confirm path now superseded by TASK-151 — delete `OrderService.confirmPayment`, `OrderRepository.markPaid`, and the `PATCH /api/orders/:id/confirm-payment` route (+ its tests) once no external consumer depends on it. | ✅ | — |
| TASK-159 | **[BUG]** Order e2e happy-path 500s — `OrderItemEntity.fromPrisma` reads `product.images[0]`. **Root cause: test-fixture defect, NOT production** — `ORDERS_INCLUDE` always selects `images` and `OrderItemRow.product.images` is required, but the mocked `makeOrder()` line item omitted `images`, so the contract-violating mock crashed every order happy-path (8–9 e2e). Fixed by adding the `images` array to the fixture + asserting `imageUrl` maps in `expectOrderShape`. order+rbac e2e 41/41 green. | ✅ | — |
| TASK-160 | **[BUG]** Pre-existing **e2e harness** instability surfaced by the full `test:e2e` run (Wave-2 features all pass in isolation): (a) **same `images[0]` fixture defect as TASK-159 in the CART path** — `cart-item.entity.ts:123` reads `product.images[0]`; `cart-guest.e2e` mock omits `images` → 500s; (b) **throttler state leaks across suites** — `cart-guest`/`auth` merge-on-login get `429` after `security.e2e` exhausts the login limit (suites don't reset the `ThrottlerModule` store). Fix the cart mock like TASK-159 and isolate/reset throttler state per suite (or raise the limit in the e2e Throttler config). **[Wave 3 / Stream C ✅]** (a) added `images: []` to cart-guest/cart/order fixtures + defensive `item.product.images?.[0]?.url ?? null` guard in `cart-item.entity.ts`. (b) **Root cause of the throttler leak was cross-worker, not cross-suite:** parallel Jest e2e workers each read `REDIS_HOST` from `.env` and shared **one real-Redis** rate-limit counter keyed by `127.0.0.1`; combined auth logins blew the 5/60s limit → spurious 429s. Fixed by forcing `REDIS_HOST=''` in `setup-e2e.ts` so every worker gets an isolated in-memory throttler; `security.e2e` still exercises the real `ThrottlerGuard`. Verified: full `npm run test:e2e -w apps/store-api` green — **18 suites / 250 tests**. | ✅ | — |

---

## Logic behind imported UI *(deferred — do the UI port first)*

> Owner's workflow: port **all** the Claude Design UI pages first (the TASK-167
> rollout), then build the logic behind the shipped UI stubs. These tasks are
> intentionally **outside the active Tier 0–4 sequence** — do not pick them up
> before the corresponding UI is in place. Grouped by the UI they back; IDs still
> use the shared monotonic counter. When one is picked up, write its
> `docs/plans/NNN-*.md` and move it into the active tiers (or work it here).

| Task ID | Description | Status | Plan |
| --- | --- | --- | --- |
| TASK-164 | **[NEW — follow-up of TASK-162]** Bestseller signal for the homepage PopularRail "Хіти продажів" tab — currently a best-effort default listing. Derive a real "хіт продажу" ordering (e.g. aggregate sold quantity from paid order items, or an admin-set flag) and expose it as a sort/filter on `GET /products`; then point the Хіти tab at it. Supersedes parked TASK-088. | ⬜ | — |
| TASK-165 | **[NEW — follow-up of TASK-162]** Device model → accessory matching for the homepage `ModelPicker` (currently a UI-only stub, submit no-ops). Needs a data model linking products to compatible device brands/models + a `GET /products` filter; then wire the picker submit to navigate to the filtered listing. | ⬜ | — |
| TASK-166 | **[NEW — follow-up of TASK-162]** Real homepage promo content + destinations — promo tiles (instalments/trade-in) and the wide promo currently link to `/products` or `#`; newsletter block shows social links with `href="#"` and no signup. Give promos real target pages/data (admin-managed) and wire the newsletter to a real signup + the TASK-154 social links. Pairs with parked TASK-089/TASK-090. | ⬜ | — |
| TASK-168 | **[NEW — follow-up of TASK-167-A]** Social sign-in (Google / Apple OAuth) — the auth slide-out renders Google/Apple buttons as UI **stubs** (toast "згодом"). Real implementation needs a backend OAuth module (provider apps, callback routes, token exchange, account linking) + wiring the buttons. Backend feature, not UI. | 🅿️ | — |
| TASK-169 | **[NEW — follow-up of TASK-167-A]** Password reset ("Забули пароль?") — the auth slide-out link is a **stub** (toast). Needs a request-reset endpoint + emailed token + reset page/form + expiry. Pairs with the TASK-103 mail-outbox infra. | 🅿️ | — |
| TASK-170 | **[NEW — Blog data; follow-up of TASK-167-D]** Blog backend module — no Blog domain exists yet; the storefront reads a static 12-post seed (`widgets/blog/model/posts.ts`). Add a Prisma `BlogPost` model (slug, title, excerpt, body, category, author, cover hue/image, `featured`, `publishedAt` + draft/published status, timestamps) + a category taxonomy (enum or `BlogCategory` table covering reviews/guides/news/tips/compare), a NestJS module (repository → service → controller, Clean Architecture, `deletedAt` soft-delete per convention), public `GET /api/blog` (category filter + search + pagination, mirroring `/products`) + `GET /api/blog/:slug`, Swagger decorators + Orval regen. Then swap the storefront static seed for the generated hooks and move filter/search/pagination server-side (URL-synced like the catalog). Unblocks real content behind the TASK-167-D UI. | ⬜ | — |
| TASK-171 | **[NEW — Blog article page; follow-up of TASK-167-D]** Storefront blog article detail page `/blog/[slug]` from the Claude Design `Article.dc.html` import; wire the TASK-167-D post-card + featured links to it (**closes the current 404 gap**). Sanitize rich-text body (DOMPurify, as TASK-153 `/info/[slug]` does), add `BlogPosting`/`Article` JSON-LD + blog-post entries in `sitemap.xml`. **Depends on TASK-170** (body/content comes from the Blog API). | ⬜ | — |
| TASK-172 | **[NEW — Blog admin CMS; follow-up of TASK-167-D]** store-admin blog management — CRUD for posts (reuse the TASK-153 `RichTextEditor` for the body), draft/publish + `publishedAt`, `featured` flag, category assignment; FSD (entity + feature + widgets) + routes (`/blog`, `/blog/new`, `/blog/[id]/edit`) + sidebar nav; Orval hooks from TASK-170. **Depends on TASK-170.** | ⬜ | — |
| TASK-173 | **[NEW — Blog wiring; follow-up of TASK-167-D]** Expose `/blog` in the storefront header + footer navigation (currently reachable only by direct URL), and give the blog newsletter block a real signup + social destinations (its three links are `href="#"`). Pairs with TASK-166 (newsletter/social wiring) + TASK-154 (`SiteContactSettings` social links) + TASK-090 (consent-gated signup). | ⬜ | — |

---

## Recently completed — Phase A & reliability close-out

> Shipped (build/lint/typecheck + automated tests green). Detailed root-cause/QA notes live in the
> linked plans and the *Pending manual QA* table above. Kept here for reference, out of the active
> sequence.

| Task ID | Summary | Status | Plan |
| --- | --- | --- | --- |
| TASK-100 | CI `npm run build` step — catches Orval drift / tree-shake failures pre-merge | ✅ | plan 044 |
| TASK-107 | Relax `AddressDto` for the UA market (phone required, country optional, etc.) | ✅ | plan 043 |
| TASK-108 | UA checkout form redesign (firstName/lastName/phone/city/deliveryAddress/notes; `onInvalid` focus) | ✅ | plan 043 |
| TASK-109 | Orval regen after `AddressDto` relaxation | ✅ | plan 043 |
| TASK-110 | Order e2e fixture — add `phone`, negative test for missing phone | ✅ | plan 043 |
| TASK-111 | ~~Smoke-verify confirmation page~~ — absorbed into TASK-119 | ✅ | plan 043 → 053 |
| TASK-112 | store-admin logout-on-reload path fix (`/auth/refresh` missing `/api`) | ✅ | — |
| TASK-113 | Customer `/account` (profile view+edit) + `/orders` history; header "Мій акаунт" link | ✅ | — |
| TASK-114 | Type the storefront order-list response (`OrderListResponseEnvelope.data` = `OrderEntity[]`) | ✅ | plan 045 |
| TASK-116 | Cart qty stepper sync — optimistic `setQty` + debounced server writes | ✅ | plan 049 |
| TASK-117 | Product search focus/debounce — controlled field, no key-remount | ✅ | plan 051 |
| TASK-118 | Guest→user cart merge — gate `useGetCart` on `isInitializing` + invalidate after refresh | ✅ | plan 052 |
| TASK-119 | Checkout confirmation redirect race — gate cart-empty guard on `isOrderSubmitted`; UA country label | ✅ | plan 053 |
| TASK-120 | Restored-tab query-hang — defensive `useRecoverStrandedQueries` (dev-only Turbopack symptom diagnosed) | ✅ | plan 054 |
| TASK-121 | Post-registration redirect + header auth-state sync | ✅ | plan 055 |
| TASK-122 | store-admin logout-on-reload re-fix | ✅ | — |
| TASK-123 | Payment/status coupling — `derivePaymentStatus` in `updateStatus` | ✅ | plan 056 |
| TASK-124 | Order status × stock — auto-restock only on pre-shipment cancel; cache eviction | ✅ | plan 057 |
| TASK-125 | Admin order list/detail — customer email + contact data (`ADMIN_ORDERS_INCLUDE`) | ✅ | plan 058 |
| TASK-126 | Product card → PDP cheapest-variant default; gallery gate confirmed correct | ✅ | plan 059 |
| TASK-128 | Seed enrichment — multi-image positions, `db:studio` fix, re-seed guide | ✅ | plan 061 |
| TASK-141 | Cross-cutting forms state-sync audit — `values`/`keepDirtyValues`; convention doc | ✅ | plan 050 |
| TASK-102 | Refresh-token cleanup — scheduled purge of revoked/expired rows | ✅ | plan 046 |
| TASK-104 (A–I) | Soft deletes / audit — `deletedAt` + repo filters + `DELETE` endpoints (`-J` still open above) | ✅ | plan 047 |
| TASK-105 (A/B/C/E) | Frontend test harness — RTL/MSW foundation, cart/checkout component tests, store-admin scaffold, CI wiring (`-D` Playwright run open above) | ✅ | plan 048 |

---

## Parked / Later

> Tracked but intentionally outside the active Tier 0–4 sequence.

| Task ID | Description | Status | Reason |
| --- | --- | --- | --- |
| TASK-034 / TASK-081 | Real payments — Stripe stub → UA providers (LiqPay/Fondy/Mono "оплата частинами") | 🅿️ | Deferred until after the first real purchase validates demand. Plan drafted: docs/plans/021-payment-integration.md |
| TASK-049 | Abandoned-cart detection + follow-up emails | 🅿️ | Marketing not a priority yet; reuses Phase B mail-outbox infra when started |
| TASK-050 | GA4 e-commerce events (+ Facebook Pixel) | 🅿️ | Needs discovery; gated behind cookie consent (TASK-090) |
| TASK-090 | Cookie consent banner + newsletter signup | 🅿️ | Prerequisite for compliant analytics; bundled with marketing push |
| TASK-082 | Mega-menu / full catalog tree in header | 🅿️ | Root-category dropdown delivered via TASK-167-A (plan 096); this parks only the **deep subcategory tree / columns** |
| TASK-083 | Category tile images on homepage (needs `Category.image` field) | 🅿️ | Behind UI rewrite |
| TASK-084 | Mobile filter drawer + sort relocation (TASK-068-E deferral) | 🅿️ | Behind UI rewrite |
| TASK-085 | Product comparison ("Порівняння") | 🅿️ | Behind UI rewrite |
| TASK-086 | Quick-view modal from product cards | 🅿️ | Behind UI rewrite |
| TASK-087 | Recently-viewed products strip (localStorage) | ✅ | Read-side + UI in TASK-162; write-side wired on the PDP in TASK-163 — fully delivered |
| TASK-088 | Bestseller / "Хіт продажу" badge (sales-driven) | 🅿️ | Superseded by TASK-164 (bestseller signal for the PopularRail "Хіти" tab) |
| TASK-089 | Contact & social bar (phone, hours, Viber/Telegram/Instagram) | ✅ | Delivered via TASK-167-A (header phone) + TASK-167-B (footer contact/social from `SiteContactSettings`), plan 096 |
| TASK-068 deferrals | Remaining redesign polish (sticky ATC bar, focus-ring audit, error→toast, primitive swaps) | 🅿️ | Behind UI rewrite |
| TASK-139 | Recommended-products carousels, admin-managed (prioritization + marketing rules) | 🅿️ | Explicit future note in QA pass; needs discovery |
| TASK-140 | Admin tables UX (shadcn sortable/filterable) + category management/visualization rethink | 🅿️ | Behind admin UI rewrite; pairs with TASK-115/TASK-147 |

---

## How to Update This File

- **Start a task:** change ⬜ → 🔄.
- **Complete a task:** change 🔄 → ✅ once build/lint/typecheck + automated tests pass. If only
  manual visual QA remains, mark ✅ and add a line to *Pending manual QA*.
- **Block a task:** change to ❌ with a note. **Park a task:** 🅿️ with a one-line reason.
- **New task IDs:** use a single monotonic counter — next free integer above the current max
  (currently TASK-173; next plain ID is TASK-174). TASK-091 and below are historical. Never reuse an old ID.
- **Plans:** add the `docs/plans/NNN-*.md` path in the Plan column when one is written.
- **Finishing a parent:** move its detailed sub-tasks into `docs/backlog-archive.md` and leave a
  one-row summary under *Completed*.
