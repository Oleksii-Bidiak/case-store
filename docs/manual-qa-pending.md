# Pending Manual QA — покроковий ранбук (TASK-101, Етап 1)

> **Для кого:** для людини, яка **не знається на QA**. Кожен пункт розписано як
> **Зроби:** (конкретні дії) → **Має бути:** (очікуваний результат).
> Базові поняття (DevTools, Network, cookie, статус-коди) пояснені в
> [`manual-qa-master.md`](./manual-qa-master.md) §«Як користуватися» — прочитай його шапку один раз.
>
> **Як відмічати:** пройшло — постав `- [x]`. Не пройшло — постав ❌ прямо в рядку і
> опиши, що сталося (наприклад: `❌ купон приймається, але сума не змінюється`).
> **Нічого не виправляй сам** — кожне ❌ потім стає задачею `fix/NNN` у `BACKLOG.md`
> (наступний вільний ID дивись у шапці BACKLOG, зараз TASK-195).

---

## Крок 0. Підготовка стенду (обов'язково перед усім)

Потрібно 4 вікна терміналу (кожна команда працює постійно). Виконуй з кореня репозиторію.

1. **Інфраструктура:** `docker compose up -d`
   → Postgres (5432), Redis (6379), Meilisearch (7700) запущені. Перевір: `docker ps` — три контейнери `Up`.
2. **Міграції + дані:** `npm run db:migrate`, потім `npm run db:seed`
   → у консолі `✓ Users: admin=…, customer=…`, 32 товари, 13 категорій.
3. **API:** `npm run start:dev -w apps/store-api` → працює на `http://localhost:3001`, Swagger: `http://localhost:3001/api/docs` _(виправлено: раніше тут стояв `/api` — то була помилка ранбука, 404 на `/api` — норма)_.
4. **Вітрина:** `npm run dev -w apps/store-client` → `http://localhost:3000`.
   сайт піднімається, але присутня помилка
   [browser] Uncaught TypeError: Cannot read properties of null (reading 'parentNode')
   at $RS (http://localhost:3000/products/universal-phone-holder-for-car-dashboard:2:76426)
   at <unknown> (http://localhost:3000/products/universal-phone-holder-for-car-dashboard:2:76495)
   [browser] Uncaught Error: Hydration failed because the server rendered text didn't match the client. As a result this tree will be regenerated on the client. This can happen if a SSR-ed Client Component used:

- A server/client branch `if (typeof window !== 'undefined')`.
- Variable input such as `Date.now()` or `Math.random()` which changes each time it's called.
- Date formatting in a user's locale which doesn't match the server.
- External changing data without sending a snapshot of it along with the HTML.
- Invalid HTML tag nesting.

It can also happen if the client has a browser extension installed which messes with the HTML before React loaded.

https://react.dev/link/hydration-mismatch

...
<RootLayout>

<html lang="uk" className="geist_a715...">
...
<QueryClientProvider client={{}}>
<QueryRecovery>
<AuthProvider>
<a>
<Header>
<AnnouncementBar>
<header className="sticky top...">
<div className="mx-auto fl...">
<div>
<HeaderSearch>
<div className="ml-auto fl...">
<LinkComponent>
<HeaderWishlistBadge>
<HeaderAuth>
<HeaderCartBadge className="ml-1">
<button type="button" onClick={function onClick} aria-label="Відкрити к..." ...>
<span>
<span className="hidden fle...">
<span>
<span className="font-mono text-sm font-bold" aria-label="Сума кошика">

-                             0 грн

*                             0 ₴
                          ...
                ...
                ...

      at <unknown> (https://react.dev/link/hydration-mismatch)
      at span (<anonymous>)
      at RootLayout (src\app\layout.tsx:65:11)

  63 | {dict.nav.skipToContent}
  64 | </a>

  > 65 | <Header />

       |           ^

  66 | <main id="main-content" className="flex-1">
  67 | {children}
  68 | </main>

5. **Адмінка:** `npm run dev -w apps/store-admin` → `http://localhost:3002`.
6. **Перевір `NEXT_PUBLIC_API_URL`** в `apps/store-client/.env.local` і `apps/store-admin/.env.local`:
   має бути `http://localhost:3001` **без** `/api` в кінці (деталі — master §Крок 1).
7. Для пошуку (пункт TASK-075) в `apps/store-api/.env` мають бути `MEILI_HOST=http://localhost:7700`
   і `MEILI_MASTER_KEY=…` (той самий, що в `docker-compose.yml`). Після зміни `.env` перезапусти API.

**Акаунти (з сіда):**

- Адмін: `admin@store.com` / `Admin123!` (адмінка на :3002)
- Покупець A: `customer@store.com` / `Customer123!`
- Покупець B: зареєструй сам на вітрині (наприклад `customerb@store.com` / `Customer123!`) —
  потрібен для перевірок бану і злиття кошиків.

**Як увімкнути темну тему для візуальних перевірок:** DevTools (F12) → `Ctrl+Shift+P` →
набери «Rendering» → у панелі знайди **Emulate CSS media feature prefers-color-scheme** → `dark`.
Сайт слідує системній темі, окремого перемикача немає.

---

## 1. Інтеграційні прогони (термінал, браузер майже не потрібен)

- [✅] **TASK-105-D — Playwright E2E.**
  **Зроби:** стек запущений (Крок 0); одноразово `npx playwright install chromium`; потім `npm run test:e2e:pw`.
  **Має бути:** 4 специфікації зелені. Якщо падають — скопіюй назву тесту й помилку в ❌.
  **виправлено 2026-07-04**: сід конструював PrismaClient без pg-адаптера і посилався на
  видалену модель ProductVariant; cart-спека кліала не ту кнопку (хедерний «Кошик» замість
  «Додати до кошика»); локальний прогін тепер серійний (dev-компіляція + спільний Redis-тротлінг
  не тягнуть 4 воркери). Прогнано: **4/4 зелені**.

- [✅] **TASK-044-I / TASK-066 / TASK-152 / TASK-137 — інтеграційні тести на реальній БД (`test:int`).**
  **Зроби:** запусти **з кореня репозиторію** (`D:\projects\store-ai`, НЕ з папки `apps/` — інакше
  `npm error No workspaces found`): `npm run test:int -w apps/store-api`.
  DATABASE_URL задавати не треба — setup-int сам цілить у `store_test`.
  Якщо падає на «column … does not exist» — онови схему тестової бази:
  `npm exec -w apps/store-api --no -- prisma db push --url "postgresql://postgres:postgres@localhost:5432/store_test" --accept-data-loss`
  **Має бути:** всі int-тести зелені (Redis-кеш hit/miss/інвалідація + дашборд-виручка/топ-товари/low-stock + unrealized-виручка).
  **виправлено 2026-07-04**: схема `store_test` була застаріла (без `products.stock`), а спеки
  відстали від TASK-142 (ProductVariant) і observability (PinoLogger у CacheService).
  Прогнано з кореня: **15/15 зелені**. (Базу `store_test2` можна видалити:
  `docker exec store_postgres psql -U postgres -c "DROP DATABASE store_test2;"`)

- [✅] **TASK-152 / TASK-137 (візуальна частина) — дашборд адмінки.**
  **Зроби:** зайди адміном на `http://localhost:3002` → Dashboard.
  **Має бути:** картки виручки показують **зароблену** (оплачені замовлення) і **незароблену**
  (замовлено, не оплачено) окремо; нижче графіків є таблиця **Топ-товари** (ранг / назва / виручка в ₴)
  і таблиця **Закінчується на складі**. Якщо все по нулях — спершу оформи 1-2 замовлення (розділ 2)
  і познач одне як PAID в адмінці.

---

## 2. Сторфронт — флоу (браузер, http://localhost:3000)

- [✅] **TASK-045-I — JSON-LD на сторінці товару.**
  **Зроби:** відкрий будь-який товар → `Ctrl+U` (вихідний код) → `Ctrl+F` → шукай `ld+json`.
  **Має бути:** блок `<script type="application/ld+json">` з `"@type":"Product"`, назвою і ціною.

- [✅] **TASK-119 — checkout від і до.**
  **Зроби:** увійди як Покупець A → додай товар у кошик → `/checkout` → заповни форму
  (місто/відділення через автопідказку НП, якщо без ключа — просто текстом) → «Далі» → перевір
  дані на кроці «Перевірка» → «Підтвердити».
  **Має бути:** редірект на `/orders/{довгий-id}/confirmation` (НЕ на порожній `/cart`);
  на підтвердженні — адреса з містом/відділенням/телефоном і «Україна», товари й суми збігаються.
  **Додатково:** відкрий `/checkout` з порожнім кошиком → має редіректити геть; розлогінься
  і відкрий `/checkout` → має вести на вхід.

      **фактично**: кнопка Далі (на сторінці /checkout) не спрацьовує або через turbopack (ну дуже довго обробляє сторінки), або кнопка не працює впринципі
      	виправлено

- [✅] **TASK-118 — злиття кошика гостя.**
  **Зроби:** розлогінься → додай 2 товари в кошик як гість → увійди як Покупець A.
  **Має бути:** товари гостя в кошику одразу, без F5; після F5 кошик не порожніє.
  **факт**: товар в кошик додається одразу, але при f5 все зкикає (див Сценарій 1 нижче)

- [❌] **TASK-126 — картка → сторінка товару (акційний варіант).**
  **Зроби:** знайди в каталозі картку з перекресленою ціною, запам'ятай ціну → клікни.
  **Має бути:** відкрився саме той (найдешевший/рекламований) варіант, ціна = ціні з картки;
  перемикання варіанта (колір/обсяг) далі працює і змінює ціну.
  **факт**: на прикладі Tempered Glass Screen Protector for iPhone 15 — Double Pack - відображається ціна для Single Pack, при відкритті сторінки товару відкривається правильний товар Double Pack із іншою ціною. потрібно перевірити логічність створення варіантів товарів у базу і їх коректне відображення.

- [❌] **TASK-132 / TASK-158 — приховання залишків.**
  **Зроби:** відкрий товар і кошик; потім F12 → Network → онови сторінку → відкрий відповідь
  запиту `products`.
  **Має бути:** на сторінках лише статус («В наявності» / «Закінчується» / «Немає»), ніде немає
  числа штук; у JSON-відповіді публічного API немає поля `stock` (лише `inStock`/`lowStock`);
  але степер у кошику не дає набрати більше, ніж є на складі (у сіді є товар
  «Braided USB-C … 2m» зі stock=0 — його кнопка «Купити» неактивна).

homepage(для widget cart)
cart:
{
"data": {
"id": "c9ff4a5d-43e3-4301-975d-d5a2b116dd0b",
"userId": null,
"items": [
{
"id": "b347596b-167f-44f8-9f39-a93485d5a2e6",
"productId": "a2651bf1-f036-4008-b5e6-ef17c74b89d1",
"quantity": 1,
"productName": "Tempered Glass Screen Protector for iPhone 15 — Single Pack",
"productSlug": "tempered-glass-screen-protector-for-iphone-15-single-pack",
"imageUrl": "https://picsum.photos/seed/tempered-glass-screen-protector-for-iphone-15-single-pack-0/800/800",
"compareAtPrice": "12.99",
"price": "8.99",
"stock": 100,
"isActive": true,
"lineTotal": "8.99",
"createdAt": "2026-07-03T15:24:45.337Z",
"updatedAt": "2026-07-03T15:24:45.337Z"
}
],
"totals": {
"subtotal": "8.99",
"itemCount": 1,
"uniqueItems": 1
},
"createdAt": "2026-07-03T13:43:13.422Z",
"updatedAt": "2026-07-03T13:43:13.422Z"
}
}

- [❌] **TASK-133 / TASK-134 — фото і лінки в кошику та замовленні.**
  **Зроби:** кошик з 2+ товарами; потім будь-яке замовлення в `/orders` → деталі. Звузь вікно до мобільної ширини.
  **Має бути:** у рядках — реальні фото товару (не сіра заглушка), клік по назві/фото веде на
  сторінку товару; верстка деталей замовлення не розсипається ні на десктопі, ні на мобільній ширині.
  **факт**: у віджеті немає посилань, але клікнувши на зображення, воно чомуть вантажиться знову.

- [✅] **TASK-130 — хедер: акаунт.**
  **Зроби:** залогінений — клікни іконку акаунта; гість — подивись той самий кут; мобільна ширина — бургер-меню.
  **Має бути:** залогінений бачить дропдаун (кабінет / замовлення / вихід), гість — вхід/реєстрацію;
  Esc закриває меню, Tab ходить по пунктах, фокус повертається на кнопку після закриття.

- [✅] **TASK-131 — скасування замовлення покупцем.**
  **Зроби:** створи замовлення (воно PENDING) → `/orders` → деталі → «Скасувати» → підтвердь.
  Запам'ятай залишок товару до/після (видно по статусу «Закінчується»/адмінці).
  **Має бути:** статус стає CANCELLED без F5; склад повернувся (в адмінці stock +N); у замовлення
  зі статусом інше ніж PENDING кнопки скасування немає.

- [⏳ розблоковано (checkout працює після TASK-195), заплановано на наступну сесію; ідея «вільні залишки» заведена як TASK-224] **TASK-124 — матриця «статус × склад» (КРИТИЧНО).**
  **Зроби:** пройди готову покрокову матрицю в [`manual-qa-master.md`](./manual-qa-master.md) §C2-a
  _(переписано 2026-07-04 під актуальні статуси: UA-лейбли TASK-129, вільні переходи + окремий
  селект оплати TASK-151 — оплата більше НЕ синхронізується зі статусом; доданий сценарій
  «оживлення» скасованого → підозра на подвійне повернення запасу, заведено як TASK-228)._
  **Має бути:** склад списується при створенні; рух статусів уперед його не змінює; скасування
  ДО відправки повертає склад; скасування після «Відправлено»/«Доставлено» і «Повернення
  коштів» — НЕ повертає; повторне скасування не нараховує склад двічі.

- [✅] **TASK-077 — точки-кольори на картці.** _(поведінку оновив TASK-167-C: quick-add по ховеру
  замінено постійною кнопкою «Купити»)._
  **Зроби:** знайди картку товару з варіантами кольорів (чохли з сіда).
  **Має бути:** на картці кольорові точки і ціна «від {N} ₴»; «Купити» додає в кошик
  дефолтний (найдешевший) варіант; якщо дефолтний варіант відсутній на складі — кнопка неактивна.

- [якось мажко описати - блюр є, але ніби все вантажиться одночасно (перевірив на 3g режимі) - тут потрібно робити оптимізацію, сторінка із 10-ма товарами вантажилась секунт з 15-20, може навіть і більше] **TASK-074 / TASK-091 — зображення.**
  **Зроби:** повільно скроль каталог (можна F12 → Network → фільтр Img); окремо: в адмінці завантаж
  у товар власний JPEG і подивись на нього на вітрині.
  **Має бути:** нижні картинки довантажуються ліниво з blur/шимером; перший ряд — одразу, без
  стрибків верстки; у консолі немає помилки про незареєстрований image-host. Завантажений JPEG
  зберігся як `.webp` (видно в Network за розширенням URL), на вітрині в нього справжній blur-up
  (розмита мініатюра, не сірий шимер); анімований GIF вантажиться і анімується як був.

- [✅ - ніби працює, але всеріно покрити тестами] **TASK-079 — купони.**
  **Зроби:** в адмінці `/discounts` створи два коди: відсотковий і фіксований (постав min-spend
  та термін дії). На вітрині в кошику застосуй валідний код; потім спробуй неіснуючий,
  прострочений і код нижче min-spend. Оформи замовлення з купоном.
  **Має бути:** валідний код показує знижку і новий підсумок; кожен невалідний — своє зрозуміле
  повідомлення; в адмінці замовлення видно `discountCode` і суму знижки; у `/discounts` лічильник
  використань +1.
  **факт**: промокоди створились, але при спробі їх вводу в корзині: Не вдалося застосувати промокод. Спробуйте ще раз. в /checkout купон застосовується
  **побажання**: тут 100% треба покрити тестами, бо варіантаів ручної перевірки безліч. склади список можливих варіантів розробки класної і сучасної системи купонів

- [✅] **TASK-076 — вішліст.**
  **Зроби:** як гість натисни сердечка на 2 товарах → F5 → перевір `/wishlist` → увійди як Покупець A.
  **Має бути:** сердечка пережили F5 (кука `wishlistToken`); після входу гостьовий список злився
  з акаунтом без дублікатів; лічильник на іконці в хедері оновлюється; видалення зі сторінки
  `/wishlist` працює.

- [❌ - усе працює при пошуку 'ihpone', при пошуку 'афйон' - ні ] **TASK-075 — пошук (потрібен Meilisearch, див. Крок 0 п.7).**
  **Зроби:** у полі пошуку в хедері набери з помилкою `ihpone`, потім `афйон`; пострілкуй
  вгору/вниз, Enter на підказці і Enter на тексті; Esc.
  **Має бути:** випадаючі підказки з правильними товарами попри помилки; стрілки ходять по
  підказках, Enter на підказці відкриває товар, Enter на тексті — `/search?q=…` з картками
  і порожнім станом для нісенітниці; Esc закриває. На мобільній ширині поле живе в бургер-меню.

- [❌ - працює, але після фіксу оформленя замовлення можна буде протестувати повністю. мені подобається, що не можна писати повнорно (викинуло помилку), але дай обєктивну оцінку цій механіці, як це зроблено на великих магазинах (можливість повторно написати і захист від спаму якщо писатимуть багато наприклад, модливість редагувати, відписувати у відповідь модератору і так далі)] **TASK-078 / TASK-106 — відгуки з модерацією.**
  **Зроби:** Покупцем A на сторінці товару залиш відгук → перевір вкладку відгуків → адмінкою
  зайди в `/reviews` і схвали → повернись на товар. Спробуй лишити другий відгук на той самий товар.
  **Має бути:** до схвалення відгук не видно; після — з'явився, середній рейтинг перерахувався;
  якщо Покупець A купував цей товар — біля відгуку бейдж підтвердженої покупки; повторний
  відгук відхиляється з повідомленням (сервер відповідає 409 — видно в Network).

- [✅] **TASK-120-E — «заморожена» вкладка (best-effort).**
  **Зроби:** відкрий каталог → згорни браузер / переключись на інші програми на 10-15 хв
  (щоб браузер заморозив вкладку) → повернись.
  **Має бути:** сторінка ожила сама (немає вічних скелетонів); звичайне перемикання вкладок не
  викликає шторм повторних запитів (Network); F5 працює. _Якщо не відтворюється — просто
  постав ✅, це захисний механізм._

- [✅] **Сценарій 1**: неавторизований користувач додає в кошик продукт, додає в улюблене продукт, авторизується, і з улюленого все вичищажться. при перезавантаженні сторінки користувача розлогінює. можливо зникають і товари в кошику.
  **виправлено**: злиття кошика/вішліста підтвердив перепроход 2026-07-04 (TASK-118/076 ✅);
  розлогін при F5 мав другу причину крім кешу — rate-limit 5/хв на `/auth/refresh` давав 429
  при серії перезавантажень, і фронт трактував це як смерть сесії (fix/196: ліміт 30/хв +
  ретрай тимчасових помилок в обох апках).
- [❌ розгадано, лишився фікс повідомлення → TASK-202] **Сценарій 2**: при реєстрації test@gmail.com із паролем testtest виоітає помилка, що email вже зареєстровано (що правильно, цей email я був створював, коли тестував реєстрацію), але при логіні вилітає помилка, що парольи хибний
  **причина**: `test@gmail.com` — акаунт, забанений тобою під час тесту TASK-150; API чесно
  відповідає 401 «Account is deactivated», але фронт показує всі 401 як «Невірний email або
  пароль» (TASK-202). Бонус: пароль `testtest` пройшов реєстрацію → парольна політика API
  слабша за фронтову → TASK-227.

**Побажання**:

- хочеться на картках товарів завжди бачити, чи вони додані в корзину (зараз це реалізовано, але тільки до перезавантаженні сторінки)
- при скролі Ваше замовлення залазить під header
- при перемиканні зображень (сторінка товану) потрібно показувати стан завантаження (індикатор для юзера, що все працбє при довгому завантаженні) (можливо така невиконана таска вже десь є)
- підправити актуальні скелетони для сторінки товару піксель в піксель до розмітки (ідентична розмітка макетів)
- якщо одні з варіантів товару є кольори, потрібно замінити їх на власне відповідні кольори круглої форми
- в каталозі потрібно додати як нові фільтри (кількість виведених на сторінку товарів, показати більше, або можливість перегляду безкінечної сторінки із віртуалізацією). такоє треба динамічні фільтри для різних товарів(наприклад для чохлів не потрібні фільтки для обєму памяті) і фіксовані
- Ціна, ₴ (слайдер у фільтрах) не синхронізована із інпутами
- сторінку /orders треба перести на сторінку аккаунта як віджет чи фічу. (зроби окремим пунктом майбутнього плану, бо я зробив макет для цього в claude design, але ще не переніс в проект)
- для слайдерів також треба infinite scroll з віртуалізацією

---

## 3. Адмінка (браузер, http://localhost:3002)

- [✅ виправлено (fix/196): вилітало через 429 на `/auth/refresh` (ліміт був 5/хв, кожен F5 його їв); тепер ліміт 30/хв + ретрай — перевірено Playwright: 6×F5 + навігація, сесія жива] **TASK-059-B / TASK-112 — вхід + тихе оновлення сесії.**
  **Зроби:** увійди адміном → F5 кілька разів → лиши вкладку на ~20 хв → поклацай розділи.
  **Має бути:** жодного вильоту на логін; у Network періодично `POST /api/auth/refresh` зі
  статусом 200 (це і є «тихе оновлення»).

- [✅] **TASK-141-B — фонове оновлення форми не з'їдає введене.**
  **Зроби:** відкрий редагування товару → зміни ціну, але НЕ зберігай → переключись на іншу
  вкладку браузера на хвилину → повернись.
  **Має бути:** твоя незбережена ціна на місці (фоновий рефетч не перезаписав поле).

- [✅] **TASK-151 — статус замовлення і статус оплати незалежні (B4).**
  **Зроби:** відкрий деталі замовлення → перемкни статус на CONFIRMED → подивись статус оплати;
  потім постав оплату PAID → подивись статус замовлення; потім скасуй неношене замовлення (до відправки).
  **Має бути:** селект статусу пропонує всі статуси (крім поточного); CONFIRMED **не** ставить
  PAID автоматично; PAID **не** рухає статус; скасування до відправки повертає склад.

- [✅] **TASK-150 — фільтр користувачів + бан (B5).**
  **Зроби:** у `/users` перемкни «Активний / Неактивний / Усі статуси»; потім забань Покупця B,
  у якого відкрита вітрина в іншому вікні, і спробуй ним оформити замовлення.
  **Має бути:** фільтр реально фільтрує (перевір по колонці статусу); забанений отримує помилку
  403 при оформленні (в Network `POST /api/orders` → 403) і після закінчення сесії не може
  увійти знову.
  **факт**: все пріцює, тільки зараз не можу перевірити, бо кнопка Далі не працює, але в аккаунт не входить, тільки було б добре замість "Невірний email або пароль." в такому випадку уточнити реальну причину.

- [✅] **TASK-136 — слаг на льоту.**
  **Зроби:** створи товар, залишивши поле slug порожнім; дивись під полем назви під час введення.
  **Має бути:** живе прев'ю слага під час набору; після збереження бекенд сам вивів slug з назви;
  мертвого поля пошуку в шапці адмінки більше немає.

- [✅] **TASK-155 — прев'ю деактивованого товару для персоналу.**
  **Зроби:** деактивуй будь-який товар → на сторінці редагування натисни «Переглянути» →
  окремо відкрий цей самий слаг на вітрині `http://localhost:3000/products/{slug}`.
  **Має бути:** прев'ю відкрилось у новій вкладці з повною сторінкою і бурштиновим банером
  «деактивовано»; на вітрині той самий слаг — 404 для покупця.

- запропонуй сучасну ієрархію категорій і варіантів, щоб це було зручно, практично як для користувачів/покупців, так і для адмінів і менеджерів(в майбутньому), бо на сайті відображається ніби непогано (я просив був зробити, як в ktc.ua), але навіть я ще не розібрався, як ці категорії і варіанти створювати і правильно підвязувати
- я взагалі не маю досвіду розробки дашбордів і crm систем, то склади демальний чеклист покращень, фішок, фіч і так далі для цього випадку.
- є косяк з категоріями - при редагування категорія злітає і при щоб зберегти зміни треба повторно її встановити

---

## 4. Зовнішні інтеграції (потрібні ключі; без ключа — пропусти і лиши позначку)

- [❌ - без ключа] **TASK-080-A…D — Нова Пошта наживо (потрібен `NP_API_KEY`).**
  **Зроби:** додай `NP_API_KEY=…` в `apps/store-api/.env`, перезапусти API → на checkout
  набери «Київ» у полі міста, вибери місто → відкрий список відділень → обери → дійди до
  підсумку. Оформи замовлення і відкрий його в адмінці.
  **Має бути:** підказки міст — реальні населені пункти; відділення підвантажились для
  вибраного міста; у підсумку з'явилась реальна вартість доставки і термін; замовлення
  зберегло відділення і `shippingCost`.

- [❌ - без ключа] **TASK-103 — надійність пошти (потрібен SMTP; інструкція по вмиканню — master §«MAIL_ENABLED», §C4).**
  **Зроби:** з `MAIL_ENABLED=true` і робочим SMTP оформи замовлення; відкрий `npm run db:studio`
  → таблиця `mail_outbox`. Потім зіпсуй SMTP-пароль, перезапусти API, оформи ще одне.
  **Має бути:** замовлення відповідає миттєво (лист не блокує); у `mail_outbox` рядок PENDING,
  за хвилину-дві стає SENT і лист приходить; із зіпсованим паролем — рядок кілька разів
  ретраїться і стає FAILED; з `MAIL_ENABLED=false` рядки просто стають SENT без відправки.

- [❌ - без ключа] **TASK-048 — Sentry (потрібен DSN).**
  **Зроби:** пропиши `SENTRY_DSN` (api) і `NEXT_PUBLIC_SENTRY_DSN` (client/admin), перезапусти.
  Зупини Postgres (`docker stop store_postgres`) і відкрий `http://localhost:3001/api/products`
  → це дасть справжню 500. Запусти Postgres назад. Потім спробуй увійти з неправильним паролем (401).
  **Має бути:** 500-помилка з'явилась у Sentry-проєкті з правильним `environment`;
  401/валідаційні помилки в Sentry НЕ потрапляють; без DSN усі три застосунки працюють як раніше.

- [❌ - є warnings, + error] **TASK-138 — чистота консолі.**
  **Зроби:** F12 → Console; відкрий мобільне бургер-меню; переглянь стартові логи API (термінал 2).
  **Має бути:** немає попередження Radix про `aria-describedby`; при вимкненій пошті в логах
  API є рядок «Mail disabled — skipping…» на рівні info; якщо бачиш попередження про
  link-preload — просто зафіксуй його текст у ❌ (це best-effort пункт).

Uncaught Error: Hydration failed because the server rendered text didn't match the client. As a result this tree will be regenerated on the client. This can happen if a SSR-ed Client Component used:

- A server/client branch `if (typeof window !== 'undefined')`.
- Variable input such as `Date.now()` or `Math.random()` which changes each time it's called.
- Date formatting in a user's locale which doesn't match the server.
- External changing data without sending a snapshot of it along with the HTML.
- Invalid HTML tag nesting.

It can also happen if the client has a browser extension installed which messes with the HTML before React loaded.

https://react.dev/link/hydration-mismatch

...
<RootLayout>

<html lang="uk" className="geist_a715...">
...
<QueryClientProvider client={{}}>
<QueryRecovery>
<AuthProvider>
<a>
<Header>
<AnnouncementBar>
<header className="sticky top...">
<div className="mx-auto fl...">
<div>
<HeaderSearch>
<div className="ml-auto fl...">
<LinkComponent>
<HeaderWishlistBadge>
<HeaderAuth>
<HeaderCartBadge className="ml-1">
<button type="button" onClick={function onClick} aria-label="Відкрити к..." ...>
<span>
<span className="hidden fle...">
<span>
<span className="font-mono text-sm font-bold" aria-label="Сума кошика">

-                             0 грн

*                             0 ₴
                        ...
              ...
              ...

  at throwOnHydrationMismatch (react-dom-client.development.js:5465:11)
  at prepareToHydrateHostInstance (react-dom-client.development.js:5561:21)
  at completeWork (react-dom-client.development.js:12924:15)
  at runWithFiberInDEV (react-dom-client.development.js:986:30)
  at completeUnitOfWork (react-dom-client.development.js:19132:19)
  at performUnitOfWork (react-dom-client.development.js:19005:11)
  at workLoopConcurrentByScheduler (react-dom-client.development.js:18982:9)
  at renderRootConcurrent (react-dom-client.development.js:18964:15)
  at performWorkOnRoot (react-dom-client.development.js:17822:11)
  at performWorkOnRootViaSchedulerTask (react-dom-client.development.js:20471:7)
  at MessagePort.performWorkUntilDeadline (scheduler.development.js:45:48)
  The resource http://localhost:3000/\_next/static/media/6bd983bd58a87a3d-s.p.0h108oidc_0fm.woff2 was preloaded using link preload but not used within a few seconds from the window's load event. Please make sure it has an appropriate `as` value and it is preloaded intentionally.
  silicon-case-iphone-17:1 The resource http://localhost:3000/\_next/static/media/caa3a2e1cccd8315-s.p.09~u27dqhyhd6.woff2 was preloaded using link preload but not used within a few seconds from the window's load event. Please make sure it has an appropriate `as` value and it is preloaded intentionally.
  silicon-case-iphone-17:1 The resource http://localhost:3000/\_next/static/media/797e433ab948586e-s.p.08e28id.o-okb.woff2 was preloaded using link preload but not used within a few seconds from the window's load event. Please make sure it has an appropriate `as` value and it is preloaded intentionally.

---

## 5. Дизайн-імпорт — візуальні проходи

> Для кожної сторінки: пройди її **у світлій і темній темі** (перемикання — Крок 0 внизу).
> Дивись на: цілісність верстки (нічого не наїжджає/не вилазить), контраст у темній темі,
> робочі інтерактиви. Мокапи для звірки — `*.dc.html` з дизайн-імпорту (за наявності локально);
> якщо мокапа під рукою немає — критерій «виглядає завершено і працює».

- [❌ - загалом все правильно, аое пару нюансів] **TASK-167-A/B/C — хедер / футер / картки.**
  **Зроби:** на будь-якій сторінці поклацай: пошук-«пігулку» (кнопка «Каталог» зліва, ввід
  тексту, лупа), гостьову кнопку «Кабінет», кнопку кошика, сердечка на картках; проскроль до футера.
  **Має бути:** панель каталогу і підказки пошуку відкриваються по черзі (не одночасно) і
  прив'язані до пігулки; «Кабінет» гостя відкриває бічну панель Вхід/Реєстрація — вхід і
  реєстрація з неї реально працюють і панель закривається; кошик відкриває міні-кошик
  (степер кількості і видалення синхронні зі сторінкою `/cart`, «Оформити» → checkout);
  темний футер: 4 колонки, соцкнопки, платіжні піли — читабельні в обох темах; сердечка є
  на «Ви переглядали» і «Схожі товари» і перемикаються.
  **факт** - футер світлий (так задумано) - у секції Ви переглядали картки не такі, як в інших місцях (не за макетом, використовується застарілий ui), і цю секцію треба замінити на такий ж слайдер як у табах секції Популярне (необхідно перевикористати)

- [✅] **TASK-167-D — блог `/blog`** _(відкривається лише прямим URL — у навігації буде після TASK-173)._
  **Зроби:** відкрий `/blog`; поклацай чипи категорій, пошук, «Показати більше статей».
  **Має бути:** hero з пошуком, чипи з лічильниками, велика featured-картка (лише без фільтра),
  адаптивна сітка, порожній стан для нісенітниці в пошуку; кожна картка відкриває свою статтю.
  **побажання**: додати до пошуку в хедері пошук і статтей - або порівну, або 3-5 статтей(найкращий результат) після знайдених товарів, розділені сепаратором, і товари і статті скроляться незалежно один від одного (статтей максимум 5, товари проскролюються)

- [✅] **TASK-171 — стаття блогу `/blog/{slug}`.**
  **Зроби:** відкрий будь-яку статтю з `/blog`; поклацай зміст (TOC) праворуч, кнопки шерингу.
  **Має бути:** шапка (бейдж/заголовок/автор), обкладинка, липкий TOC плавно скролить до
  розділів (на вузькому екрані TOC схований); «копіювати лінк» показує тост; Telegram/Facebook
  відкривають вікна шерингу; «Читайте також» веде на інші статті без 404. _Тіло поки демо —
  однакове для всіх статей, це норма до TASK-170._

- [❌ - `/legal` є, `/legal/{slug}` є тілки розмітка (/legal/[slug]/page.tsx)] **TASK-167-E/F — правові сторінки `/legal` і `/legal/{slug}`.**
  **Зроби:** переконайся, що в адмінці `/pages` є хоч одна опублікована сторінка (якщо ні — створи).
  Відкрий `/legal` → тайл → документ; поклацай нумерований TOC; натисни «Завантажити PDF» (друк).
  **Має бути:** хаб показує тайл на кожну опубліковану сторінку з «Оновлено …»; у документі
  липкий TOC підсвічує поточний розділ при скролі, заголовки автонумеровані; друк дає чистий
  документ без шапки/футера; хлібна крихта повертає на хаб.

- [✅ промокод на /cart працює (перевірено 2026-07-04 чистим браузером: TEST1 → −10%; то був той самий старий бандл, TASK-198 закрито); лишились дрібниці: «0» при ручному витиранні кількості → TASK-207; Додаткові послуги — стаб до TASK-174] **TASK-167-G — кошик `/cart`.**
  **Зроби:** наповни кошик → пограйся степером, видаленням, «Очистити кошик», промокодом
  (з TASK-079), тумблерами «Додаткові пропозиції».
  **Має бути:** двоколонкова верстка з липким підсумком; суми перераховуються миттєво;
  справжній купон застосовується; очищення питає підтвердження; тумблери послуг змінюють
  рядок «Додаткові послуги» в підсумку _(це стаб — у checkout вони не переносяться, так і треба)_;
  «Оформити» веде на `/checkout`.

- [✅] **TASK-167-H — кабінет `/account`.**
  **Зроби:** увійди Покупцем A → `/account`; зміни ім'я і телефон → збережи → F5; поклацай усі
  пункти сайдбару; вийди з акаунта через сайдбар.
  **Має бути:** липкий сайдбар з карткою користувача (ініціали/ім'я/телефон); збереження
  профілю переживає F5 (email — лише читання); «Історія замовлень»/«Обране» ведуть на
  `/orders`/`/wishlist`; «Бонуси»/«Налаштування»/«Покупки»/«Історія перегляду»/«Порівняння»
  показують акуратні заглушки «скоро» _(це стаби — норма до TASK-175)_; вихід завершує сесію.

- [✅] **TASK-167-I — хаб категорій `/categories`.**
  **Зроби:** поклацай пункти лівої рейки; клікни кілька тайлів підкатегорій.
  **Має бути:** рейка липка, активний пункт підсвічений; праворуч — заголовок, опис і тайли
  підкатегорій обраної групи; тайл відкриває каталог, відфільтрований по цій категорії
  (у каталозі — хлібна крихта і заголовок з назвою категорії); стрічка «Популярні бренди» —
  статичний стаб _(норма до TASK-189)_.

- [❌ - після етапу перевірка Перевірка після підтвердження переносиить на деталі замовлення, пункт Підтвердження можна принципі обєднати із 2 пунктом(зробити Перевірка і Підтвердження)] **TASK-167-J — checkout `/checkout` (візуальна частина; флоу — пункт TASK-119 вище).**
  **Зроби:** пройди checkout ще раз, дивлячись на верстку обох кроків.
  **Має бути:** індикатор кроків Доставка→Перевірка відповідає реальному кроку; секції-картки
  (контакти, доставка НП, оплата-стаб); липкий підсумок з аватарками товарів, промо, доставкою
  і сумою; префіл з профілю і маска `+380 …` працюють; радіо оплати і «списати бонуси» —
  неактивні заглушки _(норма до TASK-034/175)_.

- [❌ - працює, але тільки зміна Контакти із нюасном: сторінку треба перезапускати не f5(не оновлює), а через ctrl+shift+r. + знову ж таки треба крутий ux - наприклад для вибору днів і годин роботи використовувати саме форму] **TASK-167-K — інфо-хаб `/info`.**
  **Зроби:** поклацай бічну навігацію (Доставка / Гарантія / FAQ / Про нас / Контакти);
  відкрий пряме посилання `http://localhost:3000/info#contacts`; розгорни питання FAQ.
  **Має бути:** секції перемикаються і URL-hash оновлюється (діплінки працюють); акордеон FAQ
  відкривається; у «Контакти» — реальні телефон/email/години/месенджери з адмінських налаштувань
  (`/settings/contact` в адмінці — зміни там значення і перевір, що після ~години ISR або
  перезапуску client воно на сторінці); форма «Напишіть нам» — демо-стаб _(норма до TASK-177)_.

- треба продумати доречність фільтру Категорія

---

- в dev режимі nextjs дуже довго вантажить сторінки
- загальний аудит ui/ux - hover, зміна курсора, де потрібно, доступність, адаптивність.
- треба продумати і прописати user stories для найкращого досвіду використання кастомерами нашого інтернет магазину у їхньому житті.

## 6. Повторні перевірки після хвилі фіксів 2026-07-05 (TASK-199…212, 227)

> Автотести (unit + e2e + Playwright 4/4) зелені; тут — лише те, що видно тільки очима
> на запущеному стенді (Крок 0). Кожне ❌ — новий рядок у BACKLOG (наступний ID — з шапки).

- [ ] **TASK-200 — пошук кирилицею.** **Зроби:** стек з Meili (Крок 0), дочекайся старту API
      (boot сам оновлює індекс) або натисни reindex в адмінці; пошукай «айфон», «афйон», `ihpone`.
      **Має бути:** всі три знаходять iPhone-товари.
- [ ] **TASK-199 — ціна групової картки.** **Зроби:** відкрий каталог з груповими позиціями
      (Double/Single Pack). **Має бути:** картка Double Pack показує свою ціну; «від …» — лише на
      найдешевшій позиції групи. ⚠️ «Купити» з картки досі додає найдешевшу позицію — це відомий
      TASK-233, не новий баг.
- [ ] **TASK-204/207 — кошик.** **Зроби:** додай товар; у кошику клікни **назву** товару;
      потім витри кількість і клацни повз поле. **Має бути:** назва веде на сторінку товару
      (мінікошик закривається сам); порожнє поле на blur повертає попередню кількість (не «0»).
- [ ] **TASK-205 — контракт кошика.** **Зроби:** DevTools → Network → `GET /api/cart`.
      **Має бути:** в items є `maxQty`, поля `stock` немає.
- [ ] **TASK-227/202 — auth.** **Зроби:** спробуй зареєструватися з паролем `testtest`;
      потім забань юзера в адмінці і увійди ним на вітрині. **Має бути:** реєстрація не проходить
      з підказкою про політику пароля; логін баненого показує «Обліковий запис деактивовано…»,
      а не «Невірний email або пароль».
- [ ] **TASK-201 — категорія в адмінці.** **Зроби:** відкрий редагування категорії з батьком
      через F5 (холодний кеш), нічого не міняй, «Зберегти». **Має бути:** батько на місці до і
      після збереження.
- [ ] **TASK-206/208 — checkout і фільтри.** **Зроби:** у checkout проскроль вниз; у каталозі
      потягай слайдер цін і повводь числа в поля min/max. **Має бути:** «Ваше замовлення» не
      ховається під хедер; слайдер та інпути синхронні в обидва боки, некоректні числа
      підрізаються на blur.
- [ ] **TASK-209 — /legal.** **Зроби:** в адмінці `/pages` створи і опублікуй сторінку з
      текстом → відкрий `/legal/{slug}`. **Має бути:** контент відрендерено (TOC по `<h2>`,
      нумерація). Попередній ❌ «тільки розмітка» був через порожню базу — seed не створює сторінок.
- [ ] **TASK-210 — картинки на 3G (prod).** **Зроби:** `npm run build -w apps/store-client`,
      `npm run start -w apps/store-client`; DevTools → Network → Fast 3G; відкрий каталог і скроль.
      **Має бути:** картинки нижче екрана вантажаться в міру скролу (не всі одразу), перші екрани
      помітно легші за dev-режим.
- [ ] **TASK-211 — «Ви переглядали».** **Зроби:** відкрий кілька товарів → повернись на
      головну. **Має бути:** секція виглядає як слайдер «Популярного» з актуальними картками;
      сердечка і «Купити» працюють.
- [ ] **TASK-212 — шрифти (prod).** **Зроби:** на тому ж prod-стенді відкрий консоль на
      головній/каталозі/PDP. **Має бути:** нуль preload-ворнінгів (у dev вони лишаються — відомий
      артефакт, не баг).
- [ ] **TASK-233 — «Купити» з картки (додано 2026-07-05).** **Зроби:** у каталозі знайди групу
      (Double/Single Pack), натисни «Купити» на **дорожчій** позиції → відкрий кошик.
      **Має бути:** у кошику саме та позиція, на якій тиснув (не найдешевша).
- [ ] **TASK-231 — wishlist.** **Зроби:** додай товар у «Обране»; DevTools → Network →
      `GET /api/wishlist`; поклацай фільтр «В наявності». **Має бути:** в items є `maxQty`,
      нема `stock`; фільтр працює як раніше.
- [ ] **TASK-232 — товар у адмінці.** **Зроби:** відкрий редагування товару з категорією і
      групою через F5 (холодний кеш), нічого не міняй, «Зберегти». **Має бути:** категорія і
      група на місці; «Без групи» досі очищає групу.
- [ ] **TASK-234 — липкі сайдбари.** **Зроби:** проскроль `/cart`, `/categories`, `/account`,
      `/info`, статтю блогу і `/legal/{slug}`. **Має бути:** жоден сайдбар/TOC не ховається
      під хедер; кліки по TOC приводять точно до розділу.

## 7. Перевірки після UX-хвилі 2026-07-05 (TASK-213…216, 221)

- [ ] **TASK-213 — стан «В кошику» на картках.** **Зроби:** додай товар у кошик з картки в
      каталозі, онови сторінку (F5). **Має бути:** картка показує «В кошику» (зелений
      контур + галочка) і після перезавантаження; клік по ній відкриває міні-кошик.
- [ ] **TASK-214 — PDP-галерея.** **Зроби:** DevTools → Network → Fast 3G; на PDP поперемикай
      фото. **Має бути:** спінер з'являється лише коли фото вантажиться помітно (>120 мс),
      швидкі перемикання з кешу не блимають; скелетон → контент без стрибка лейауту
      (перевір мобільну і десктопну ширину).
- [ ] **TASK-215 — свотчі кольорів.** **Зроби:** відкрий PDP товару з кольорами (кабелі
      Navy Blue, чохли Frosted Black). **Має бути:** круглі свотчі реальних кольорів
      (світлі — з бордером, вибраний — з кільцем), назва кольору в легенді; на картках
      каталогу кольорові крапки теж реальні.
- [ ] **TASK-216 — каталог.** **Зроби:** у каталозі натисни «Показати ще N товарів» кілька
      разів; поклацай чіпси категорій над сіткою на вузькому екрані (горизонтальний скрол).
      **Має бути:** сторінки додаються без стрибка скролу; зміна фільтра/категорії скидає
      доклеєне до базової сторінки (це очікувано); нумерація пагінації внизу працює як
      раніше; кнопка нормально виглядає в темній темі.
- [ ] **TASK-221 — графік роботи в адмінці.** **Зроби:** відкрий Налаштування → Контакти;
      якщо там довільний текст — він має лишитися в raw-режимі з кнопкою переходу на
      структурований редактор; заповни дні, збережи, перевір футер/`/contact` на сторфронті.
      **Має бути:** рядок виду «Пн–Пт: 9:00–18:00; Сб: 10:00–16:00; Нд: вихідний»;
      зверни увагу: парсибельний старий рядок при збереженні канонікалізується
      («9:00 – 20:00» → «9:00–20:00») — це задумано.

## 8. Етап 2 — контент-платформа CRM (TASK-185/187, 170/172/173, 186, 177, 188)

> Усі автоматичні гейти зелені (unit 845+358+172, supertest e2e 266/266 **серійно**,
> Playwright 4/4). Нижче — живі перевірки на запущеному стенді. Схема запушена в
> `store_dev` і засіяна (Banners 6, Blog 5 категорій / 12 постів).
> **Env для ревалідації** (без них ISR-очистка мовчки no-op — не помилка): у store-api
> `PUBLISHING_CRON` (деф. `* * * * *`), `STOREFRONT_REVALIDATE_URL`
> (напр. `http://localhost:3000/api/revalidate`), `REVALIDATE_SECRET`; у store-client той самий
> `REVALIDATE_SECRET`.

- [ ] **TASK-187/185 — публікація сторінок + санітизація.** **Зроби:** в адмінці Сторінки
      створи сторінку зі статусом «Чернетка» → на сторфронті `/legal/<slug>` має бути 404;
      переключи на «Опубліковано» → сторінка зʼявляється (за наявності env — одразу через
      ревалідацію, без hard-reload); у контенті спробуй вставити `<script>` — має вирізатись.
- [ ] **TASK-187 — планування + cron.** **Зроби:** постав сторінці/посту статус «Заплановано»
      з часом на 1–2 хв уперед. **Має бути:** лишається прихованою; коли час мине, cron
      `PublishingScheduler` перемикає її на PUBLISHED автоматично (перевір за ~хвилину).
- [ ] **TASK-170/172/173 — блог.** **Зроби:** відкрий `/blog` (сторфронт) — має показати
      реальні пости з БД (не статичний сід), фільтр за категорією + пошук + пагінація працюють
      через URL; відкрий пост `/blog/<slug>`. В адмінці `/blog` створи/відредагуй пост через
      RichTextEditor, зроби featured, опублікуй — має зʼявитись на сторфронті. Перевір лінк
      «Блог» у хедері та футері.
- [ ] **TASK-186 — банери головної.** **Зроби:** в адмінці `/banners` створи банер на кожен
      слот (HERO_SLIDE / PROMO_TILE / PROMO_BANNER / ANNOUNCEMENT_BAR), опублікуй. **Має бути:**
      головна показує їх у відповідних зонах; якщо слот порожній — рендериться попередній
      хардкод-фолбек (головна ніколи не порожня). Смуга оголошень тягнеться з root-layout.
- [ ] **TASK-177 — контактні звернення.** **Зроби:** на `/contact` заповни й надішли форму —
      має бути справжній сабміт (не заглушка) з екраном успіху; швидко надішли 6 разів поспіль —
      6-й має впертись у ліміт (429) з дружнім повідомленням. В адмінці `/messages` звернення
      видно, лічильник непрочитаних у сайдбарі росте, можна позначити «прочитане»/«в архів» і
      додати внутрішню примітку. Перевір і форму на info-support сторінці.
- [ ] **TASK-188 — розсилка.** **Зроби:** на головній (блок «−10%») і промо-блоці введи email,
      підпишись — екран успіху; підпишись тим самим email ще раз — теж успіх (ідемпотентно, без
      дубля/помилки). В адмінці `/subscribers` запис видно; натисни «Експорт CSV» — має
      завантажитись файл `email,status,source,createdAt`.
- [ ] **Регресія головної/навігації.** Головна, `/cart`, checkout, каталог виглядають як раніше;
      нові лінки (Блог) не ламають хедер/футер у світлій і темній темі.

## 9. Етап 3 — Фаза 0: підкатегорійний rollup (TASK-236)

> Автотести зелені (store-api unit 858, e2e 269, int 25; store-admin 179; store-client 364;
> typecheck + lint чисті). Нижче — те, що видно лише на запущеному стенді (Крок 0). Схему
> `metaTitle`/`metaDescription` вже запушено в `store_dev` і `store_test` через `prisma db push`
> (міграція `migrate dev` не пройшла через дрейф Етапу 2 — це очікувано, `schema.prisma` є
> джерелом правди).

- [ ] **Meilisearch reindex (обов'язково після деплою).** Документ індексу змінив форму
      (`categoryId` → `categoryIds[]`, self + всі предки). **Зроби:** підніми Meili (Крок 0 п.7),
      перезапусти API (boot сам робить best-effort reindex) **або** натисни reindex в адмінці
      (`POST /api/admin/search/reindex`). **Має бути:** reindex проходить без помилок; майбутній
      категорійний фільтр у пошуку піднімає підкатегорії (сьогодні `/search` фільтрує лише
      `isActive` — це forward-compatible зміна, не regресія).
- [ ] **Rollup у каталозі.** **Зроби:** на вітрині `/products` клікни чіп кореневої категорії
      (напр. «Чохли»), у якій товари лежать у підкатегоріях. **Має бути:** сітка показує товари
      з усіх підкатегорій (раніше було порожньо); під активним кореневим чіпом зʼявляється
      другий рядок чіпів прямих підкатегорій; клік по підкатегорії звужує вибірку, `?categoryId=`
      несе id підкатегорії.
- [ ] **Адмінка — вибір leaf-категорії у товарі.** **Зроби:** відкрий створення/редагування товару
      → селект «Категорія». **Має бути:** у списку лише листові категорії (без гілок), з відступом
      за глибиною; неактивні leaf теж доступні; сидинг категорії при F5-редагуванні не злітає
      (guard TASK-201/232 не зламано).
- [ ] **Адмінка — SEO-поля категорії.** **Зроби:** у формі категорії заповни «SEO-заголовок» і
      «SEO-опис», збережи, перевідкрий. **Має бути:** значення збереглись і підтягнулись назад;
      порожні поля на update чистять значення (null). _Рендер у `<head>` вітрини — поза обсягом
      цього таска (Phase D)._

## 10. Етап 3 — Фаза B: сумісність пристроїв (TASK-190, plan 111)

> Код змерджено у `feature/190-device-compat`. Схема (`DeviceBrand`/`DeviceModel`/
> `ProductDeviceCompat` + relation `Product.deviceCompat`) — у `schema.prisma`; міграцію та сід
> не запускав (спільна БД). Нижче — обовʼязкові кроки на живому стенді.

- [ ] **Міграція БД (обовʼязково).** **Зроби:** `npx prisma migrate dev --name add-device-compat`
      (або `prisma db push` на dev). **Має бути:** зʼявляються таблиці `device_brands`,
      `device_models`, `product_device_compat` без помилок.
- [ ] **Сід моделей (обовʼязково).** **Зроби:** `npm run db:seed -w apps/store-api`. **Має бути:**
      сід проходить чисто та ідемпотентно (повторний запуск не дублює); зʼявляються бренди
      Apple/Samsung/Xiaomi і їх моделі, згруповані за `series`.
- [ ] **Meilisearch reindex (після сіду/бекфілу compat).** Документ індексу отримав нове поле
      `deviceModelIds[]` + `filterableAttributes`. **Зроби:** reindex через boot API або
      `POST /api/admin/search/reindex`. **Має бути:** reindex без помилок.
- [ ] **Бекфіл compat (ручний, за потреби).** Автоінференс сумісності не входить в обсяг —
      призначай сумісність товарам через адмінку (нижче) або окремим скриптом з ручним ревʼю.
- [ ] **Адмінка — довідники пристроїв.** **Зроби:** сайдбар «Пристрої» → вкладки «Бренди»/«Моделі»;
      створи/відредагуй бренд і модель, перемкни статус. **Має бути:** slug авто-генерується;
      селект бренду в формі моделі показує наявні бренди; toggle приховує/показує; сидинг форми
      при F5-редагуванні не злітає.
- [ ] **Адмінка — сумісність товару + bulk «на всю групу».** **Зроби:** відкрий редагування товару
      → секція «Сумісні пристрої»; постав галочки моделей, «Зберегти»; для товару з групою натисни
      «Застосувати до всіх позицій групи». **Має бути:** набір зберігається (перевідкриття показує
      його); bulk показує тост із кількістю оновлених позицій; невідомий id → 400 (не має статись
      з UI).
- [ ] **Вітрина — homepage ModelPicker.** **Зроби:** на головній обери бренд → модель → «Підібрати».
      **Має бути:** селект моделі заблокований до вибору бренду й скоупиться до бренду; сабміт веде
      на `/products?deviceModelId=<id>` і сітка фільтрується.
- [ ] **Вітрина — каталог-фільтр + чіп.** **Зроби:** у сайдбарі каталогу «Сумісний пристрій»
      (бренд→модель); поєднай з категорією/ціною. **Має бути:** URL несе `?deviceModelId=`; чіп
      «Пристрій: X» знімається; «Очистити все» прибирає і його.
- [ ] **Вітрина — PDP cross-sell.** **Зроби:** відкрий товар із заданою сумісністю. **Має бути:**
      секція «Сумісні аксесуари» з іншими товарами під ту саму модель (поточний виключений);
      для товару без compat секція не рендериться.
- [ ] **e2e (serial).** **Зроби:** `npm run test:e2e -w apps/store-api -- --runInBand` після
      міграції. **Має бути:** зелено (плюс перевірка single/group/invalid-id/idempotent сценаріїв
      compat, якщо додаси e2e-спеку).

## Після проходу

1. Для кожного ❌ заведи рядок у `BACKLOG.md` (Етап 1, `fix/NNN`, ID з лічильника) з коротким
   описом відтворення. Критичні (гроші/дані/безпека/checkout) — виконуються одразу.
2. Пункти, які не вдалося перевірити без ключів (`NP_API_KEY`, SMTP, Sentry DSN), залиш
   невідміченими з позначкою `⏸ немає ключа` — вони не блокують Етап 2.
3. Коли всі рядки або ✅, або перетворені на задачі — TASK-101 у BACKLOG можна закривати.
