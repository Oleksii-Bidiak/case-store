# Ранбук сесій: хвилі після живого прогону 2026-08-27

> **Для кого:** власник, який запускає сесії Claude Code одну за одною. Кожен промпт нижче
> самодостатній — копіюється цілим блоком у нову сесію. Стан і порядок звірені з репозиторієм
> 2026-09-11 (хвиля 173 змержена, B-1/B-3/B-5/B-10 вирішені, плани 180–182 створені).
> Джерела: [тріаж](reviews/2026-08-27-demo-run-triage.md) §7/§9/§10, плани
> [173](plans/173-demo-run-fix-wave.md)…[182](plans/182-catalogue-facets.md), перепровірка —
> [`qa-recheck.md`](qa-recheck.md).

## Перед кожною сесією

1. Claude Code з кореня `D:\projects\store-ai`, гілка `develop`, `git pull`. Worktree сесія створює
   сама (слово «worktree» у промпті вмикає інструмент; `worktree.baseRef=head` у налаштуваннях,
   інакше гілка пішла б від застарілого `main`).
2. Модель Opus для виконання й мержу. Брейншторми — будь-яка.
3. Паралельні сесії — окремі вікна з того самого кореня.
4. Слово **ultracode** у промпті вмикає багатоагентний режим; без нього сесія працює звичайно.

## Порядок

S0 → D → S6a ∥ S6b ∥ S6c → M(175), M(174), M(176) → S8 ∥ S9 → S10 ∥ S11 → S12 → S7, S7′ → S13 →
S14 → D → перепровірка → Z → S15. Брейншторми B у паузах.

Локального стенда немає: `[🔁]` у `qa-recheck.md` означає «змержено», перевіряється лише після
D; таблиця «Деплої» каже, який коміт на стенді.

## Промпти

### S0 — червоний CI і хвости ревʼю (TASK-460…467)

```
Створи worktree fix-460-ci-and-review-tail і працюй у ньому. Перед першою збіркою виконай у worktree: npm install, npm run db:generate, npm run generate:api.

Увійди в plan mode. Прочитай у BACKLOG.md рядки TASK-460…467 (розділ плану 179) і docs/plans/179-quality-security-docs-infra.md. Склади план сесії: спершу 460 і 461 (CI червоний), далі 464 (безпека, fail-closed для newsletter за зразком contact.controller.ts), 466 (нормалізація телефонів через phoneDigits() + бекфіл наявних рядків міграцією), 465, 467, 462, 463. Для 463 — спершу відтворити флейк повним прогоном Playwright і зафіксувати механізм, лише потім правити. Покажи план, чекай схвалення.

Після схвалення виконуй по одній задачі агентом build із верифікацією тестами; коміт на задачу; jest --runInBand; e2e серійно. Наприкінці повні тести, git merge develop у гілку, мерж у develop без squash, вийди з worktree з видаленням (ExitWorktree remove). Звіт: чи зелений CI на develop після пушу, що лишилось.
```

### D — деплой на демо

```
Задеплой поточний develop на демо-стенд за docs/deploy/03b-test-deploy-no-domain.md, розділ «Перезалив на вже сіджений сервер». Після успішного перезаливу допиши рядок у таблицю «Деплої» в шапці docs/qa-recheck.md: дата, коміт develop, які задачі увійшли з часу попереднього деплою. Якщо це перший деплой після хвилі 173, виконай діагностику з TASK-401 п.1: лог throttler у store-api за час прогону 27.08. Закоміть docs у develop. У звіті: список чеків із [🔁], які тепер можна проходити, згруповані за зонами.
```

Після D власник проходить `[🔁]` у `qa-recheck.md`.

### S6a — хвиля 174, вітрина

```
Створи worktree feature-410-storefront-ux і працюй у ньому. Перед першою збіркою виконай у worktree: npm install, npm run db:generate, npm run generate:api.

Увійди в plan mode. Прочитай docs/reviews/2026-08-27-demo-run-triage.md §7, §9, §10 і docs/plans/174-storefront-ux-wave.md. Рішення B-10 уже є (план 178, «Рішення 2026-09-11 (B-10)»), тож TASK-414 роби повністю, включно з мультивибором ?specs=k:v1,v2;k2:v3 і inStock; TASK-217 у цій сесії не роби. Склади план сесії: порядок задач за розділом «Порядок» плану, кластери для паралелі без перетину файлів; задачі 410, 412, 415, 416 виконує агент designer з опорою на docs/design-system.md. Покажи план, чекай схвалення.

Після схвалення: ultracode, агент на кластер, верифікатор на задачу. Правила: коміт на задачу; сусідні знахідки лише в BACKLOG; jest --runInBand; e2e серійно; нові ключі словника додавай у кінець відповідних блоків dictionary.ts, бо паралельно йдуть хвилі 175 і 176. Наприкінці повні тести, [🔁] у docs/qa-recheck.md за Додатком А, звіт. Із worktree не виходь.
```

### S6b — хвиля 175, адмінка як CRM

```
Створи worktree feature-421-admin-crm і працюй у ньому. Перед першою збіркою виконай у worktree: npm install, npm run db:generate, npm run generate:api.

Увійди в plan mode. Прочитай docs/reviews/2026-08-27-demo-run-triage.md §7, §9, §10 і docs/plans/175-admin-crm-wave.md. TASK-431 переїхав у план 180 — тут його не роби. Склади план сесії: порядок задач за розділом «Порядок» плану, кластери для паралелі без перетину файлів. Покажи план, чекай схвалення.

Після схвалення: ultracode, агент на кластер, верифікатор на задачу. Правила: коміт на задачу; сусідні знахідки лише в BACKLOG; jest --runInBand; e2e серійно; міграції Prisma лише через npm run db:migrate з осмисленою назвою; нові ключі словника додавай у кінець відповідних блоків dictionary.ts, бо паралельно йдуть хвилі 174 і 176. Наприкінці повні тести, [🔁] у docs/qa-recheck.md за Додатком А, звіт. Із worktree не виходь.
```

### S6c — хвиля 176, контент і SEO

```
Створи worktree feature-432-content-seo і працюй у ньому. Перед першою збіркою виконай у worktree: npm install, npm run db:generate, npm run generate:api.

Увійди в plan mode. Прочитай docs/reviews/2026-08-27-demo-run-triage.md §7, §9, §10 і docs/plans/176-content-seo-wave.md. Склади план сесії: порядок задач за розділом «Порядок» плану, кластери для паралелі без перетину файлів; у TASK-434 вставку зображень пропусти, якщо TASK-424 ще не змержено у develop. Покажи план, чекай схвалення.

Після схвалення: ultracode, агент на кластер, верифікатор на задачу. Правила: коміт на задачу; сусідні знахідки лише в BACKLOG; jest --runInBand; e2e серійно; міграції лише через npm run db:migrate; нові ключі словника додавай у кінець відповідних блоків dictionary.ts, бо паралельно йдуть хвилі 174 і 175. Наприкінці повні тести, [🔁] у docs/qa-recheck.md за Додатком А, звіт. Із worktree не виходь.
```

### M — ревʼю і мерж хвилі (шаблон)

Замінити три поля в кутових дужках. Порядок мержу після S6: 175 → 174 → 176.

```
Увійди у вже створений worktree <feature-421-admin-crm> (git worktree list покаже шлях). Увійди в plan mode. Прочитай <docs/plans/175-admin-crm-wave.md> і git log гілки проти develop. Запусти /review на diff гілки з фокусом: <безпека нових ендпоінтів завантаження, міграції без втрати даних, контракти DTO>. Кожну знахідку класифікуй: виправити зараз (лише якщо це дефект цієї ж хвилі) або рядок у BACKLOG. Покажи список і чекай схвалення.

Після схвалення: виправ погоджене, повтори повні тести, зроби git merge develop у гілку і виріши конфлікти (у dictionary.ts зберігай ключі обох сторін), змерж гілку у develop без squash, вийди з worktree з видаленням (ExitWorktree remove). Звіт: що змержено, які чеки в docs/qa-recheck.md тепер [🔁]. На демо не заливай.
```

Фокуси ревʼю: 174 — адаптив без горизонтального скролу, CSS-рефактор теми, кеш-ключі фільтрів;
176 — порядок тирів resolveSeo, міграція Page.kind, санітайзер після нових розширень редактора;
177 — межі памʼяті при завантаженні, ліміти Caddy; 180 — машина станів оплати, публічна проєкція
без PII, fail-closed на lookup; 181 — інваріант єдиного власника, правило рівнів на всіх чотирьох
дверях, бекфіл прав; 182 — SEO-маршрути без ферми сторінок, продуктивність агрегатів.

### S8 — хвиля 177, медіа (після M(175))

```
Створи worktree feature-439-media і працюй у ньому. Перед першою збіркою виконай у worktree: npm install, npm run db:generate, npm run generate:api.

Увійди в plan mode. Прочитай docs/reviews/2026-08-27-demo-run-triage.md §7, §10 і docs/plans/177-media-pipeline.md. Склади план сесії за розділом «Порядок» плану: 439 → 440 → 442 → 441; TASK-441 медіатека лише якщо попередні три зелені, інакше залиш ⬜. Покажи план, чекай схвалення.

Після схвалення виконуй агентом build із верифікацією тестами на кожну задачу; коміт на задачу; jest --runInBand; e2e серійно. Наприкінці повні тести, [🔁] у docs/qa-recheck.md за Додатком А, звіт. Із worktree не виходь.
```

### S9 — TASK-446, відгуки

Замінити дві кутові дужки рішеннями.

```
Створи worktree feature-446-reviews і працюй у ньому. Перед першою збіркою виконай у worktree: npm install, npm run db:generate, npm run generate:api.

Увійди в plan mode. Прочитай docs/plans/178-brainstorms.md розділ B-4 повністю: рішення 1–7 прийняті, два відкриті питання вирішено так: відповіді на відгуки — <лише магазин / магазин і автор>; імʼя автора — <перша літера прізвища / лишити «Покупець»>. Це критичний модуль, тож працюй агентом tdd-agent у режимі Red→Green→Refactor. Склади план: міграція (isActive → ratingVisible + textStatus; ReviewReply, якщо відповіді потрібні), API (PATCH /reviews/:id, throttle 5/год на акаунт і 20/добу на IP, неврахування оцінок без підтвердженого email, сигнал на дашборд «Потребує дії», приховування всіх оцінок акаунта модератором), вітрина (список лише схвалених з текстом, пагінація по 10, дописування тексту до оцінки), адмінка (відхилити текст, лишивши оцінку). Покажи план, чекай схвалення, потім виконуй. Тести --runInBand, e2e серійно, коміт на крок, [🔁] на SF-PDP-20 і AD-CRM-18, із worktree не виходь.
```

### S10 — хвиля 180, замовлення після B-1 і B-5 (після M(175) і S0)

```
Створи worktree feature-431-orders-b1-b5 і працюй у ньому. Перед першою збіркою виконай у worktree: npm install, npm run db:generate, npm run generate:api.

Увійди в plan mode. Прочитай docs/plans/180-orders-after-b1-b5.md, а потім у docs/plans/178-brainstorms.md розділи «Рішення 2026-09-11 (B-1)» і «Рішення 2026-09-11 (B-5)» — там аргументація кожного рішення. Перевір, що TASK-466 уже в develop (передумова 483). Склади план сесії за кластерами O1…O5 плану 180: O1 перший, O2 ∥ O3 ∥ O4 після нього, O5 наприкінці; для кожного кластера випиши файли і перевір відсутність перетинів. Покажи план, чекай схвалення.

Після схвалення: ultracode, агент на кластер, верифікатор на задачу за розділом «Приймання по задачах». Правила: коміт на задачу; сусідні знахідки лише в BACKLOG; jest --runInBand; e2e серійно; міграції лише через npm run db:migrate. Наприкінці повні тести, [🔁] у docs/qa-recheck.md за Додатком А, звіт. Із worktree не виходь.
```

### S11 — хвиля 181, модель доступів (після M(175); паралельно з S10)

```
Створи worktree feature-474-access-model і працюй у ньому. Перед першою збіркою виконай у worktree: npm install, npm run db:generate, npm run generate:api.

Увійди в plan mode. Прочитай docs/plans/181-access-model.md, docs/reviews/2026-09-11-access-model.md і в docs/plans/178-brainstorms.md розділ «Рішення 2026-09-11 (B-3)». Хвиля послідовна по API: P1 → P2 → P3, потім P4 ∥ P5. Це критичний модуль (auth), тож P1–P3 роби агентом tdd-agent: спершу тести на вісім інваріантів із плану, потім код. Склади план сесії з переліком файлів на кожен крок і покажи, чекай схвалення.

Після схвалення виконуй крок за кроком; після P2 регенеруй API (npm run generate:api) і перевір admin-shell-guard та useAuth; сідер має ставити isOwner сідовому адміну. Коміт на задачу; jest --runInBand; e2e серійно, RBAC-сюїти повністю; Playwright admin-проєкт. Наприкінці повні тести, [🔁] у docs/qa-recheck.md за Додатком А, звіт. Із worktree не виходь.
```

### S12 — хвиля 182, фасети (після M(174))

```
Створи worktree feature-487-catalogue-facets і працюй у ньому. Перед першою збіркою виконай у worktree: npm install, npm run db:generate, npm run generate:api.

Увійди в plan mode. Прочитай docs/plans/182-catalogue-facets.md і в docs/plans/178-brainstorms.md розділ «Рішення 2026-09-11 (B-10)» з таблицями порівняння з ринком. Перевір, що TASK-414 із мультивибором уже в develop. Склади план сесії за кластерами F1 → F2 → F3; якщо TASK-420 (слаги) уже виконано, узгодь маршрути F3 із його словником. Покажи план, чекай схвалення.

Після схвалення виконуй агентом build із верифікацією за розділом «Приймання»; коміт на задачу; jest --runInBand; int-тести репозиторію на лічильники. Наприкінці повні тести, [🔁] у docs/qa-recheck.md на SF-CAT-11/13/14, звіт. Із worktree не виходь.
```

### S7 — макет для TASK-217

```
Використай скіл design. Завдання: макет розділу «Мої замовлення» всередині кабінету покупця вітрини (TASK-217, docs/plans/174-storefront-ux-wave.md). Артборди: список замовлень у /account на десктопі і на 390px, деталі замовлення на десктопі і на 390px. Візуальна мова: docs/design-system.md і поточний /account (widgets/account у store-client). Спершу опиши структуру артбордів текстом і чекай мого «так», потім створюй канвас. Реалізацію не починай: я спершу поправлю макет.
```

### S7′ — реалізація TASK-217 після правок макета

```
Створи worktree feature-217-orders-in-account і працюй у ньому. Перед першою збіркою виконай у worktree: npm install, npm run db:generate, npm run generate:api. Увійди в plan mode. Реалізуй TASK-217 за макетом <посилання на канвас> і розділом TASK-217 у docs/plans/174-storefront-ux-wave.md: історія й деталі замовлень стають секціями /account/orders і /account/orders/[id], старі /orders* редіректять 308, TASK-419 про повернення після входу врахувати. Покажи план, чекай схвалення, далі виконуй агентом designer. Тести --runInBand, коміт на крок, [🔁] у docs/qa-recheck.md, із worktree не виходь.
```

### S13 — хвиля 179, безпека, доки, моніторинг, пайплайн

```
Створи worktree chore-452-quality і працюй у ньому. Перед першою збіркою виконай у worktree: npm install, npm run db:generate, npm run generate:api.

Увійди в plan mode. Прочитай docs/plans/179-quality-security-docs-infra.md. У цій сесії — TASK-452, 453, 454, 455; TASK-456 і 457 не роби. Склади план сесії: 452 і 453 незалежні, 454 — переважно доки й конфіг, 455 після стабілізації Playwright. Покажи план, чекай схвалення.

Після схвалення: ultracode, агент на задачу, верифікатор на задачу. Коміт на задачу; сусідні знахідки лише в BACKLOG. Наприкінці повні тести, звіт. Із worktree не виходь.
```

### S14 — TASK-456, ревʼю коду

```
Увійди в plan mode. Виконай TASK-456 за docs/plans/179-quality-security-docs-infra.md: запусти агентів code-reviewer по модулях (store-api: order, cart, discount, auth, product; store-admin і store-client по шарах FSD). Фокус із плану: дублювання, порушення Clean Architecture і FSD, мертвий код, міни даних на кшталт @IsUUID(4). Результат: docs/reviews/<дата>-code-review.md за форматом docs/reviews/2026-07-13-full-project-review.md і рядки в BACKLOG з наступного вільного ID. Коду не чіпай, docs закоміть у develop.
```

### S15 — TASK-457, staging

```
Прочитай docs/plans/179-quality-security-docs-infra.md розділ TASK-457 і docs/reviews/2026-08-27-demo-run-triage.md §8. Хост staging: <адреса>. Підстав його у curl-скрипти, прожени їх сам і запиши результати в docs/reviews/<дата>-staging-run.md. Що не можна перевірити скриптом, залиш мені списком чеків із docs/qa-recheck.md, позначених «⛔ staging», у порядку зон. Закоміть у develop.
```

### B — брейншторми, що лишились

Замінити розділ і задачу: B-6 (TASK-448), B-2 (TASK-444), B-9 (TASK-451), B-8 (TASK-450),
B-7 (TASK-449). Порядок — саме такий.

```
Використай скіл superpowers:brainstorming. Тема: docs/plans/178-brainstorms.md, розділ <B-6 (TASK-448)>. Спершу коротко перекажи «Що є» і перевір у коді, що воно досі так. Задавай мені питання з розділу по одному, з варіантами і своєю рекомендацією; не переходь далі без моєї відповіді. Наприкінці запиши рішення у план 178 під «Рішення <дата>», заведи реалізаційні задачі в BACKLOG з наступного вільного ID під власним планом docs/plans/<наступний номер>, закоміть docs у develop. Коду не чіпай.
```

### Z — закриття задач після перепровірки

```
Прочитай docs/qa-recheck.md. Для кожної задачі з Додатку А, у якої всі чеки позначені [✅], постав ✅ у BACKLOG.md. Для чеків із [❌ …] створи рядки в BACKLOG з наступного вільного ID і перенеси текст «що бачив» у docs/reviews/2026-08-27-demo-run.md як нові 🐞. Закоміть docs у develop і онови памʼять проєкту: що закрито, next ID, що відкрито.
```

## Два зауваження

1. S0 до S6: з червоним CI мержі трьох хвиль підуть наосліп.
2. Після S0 і D можна одразу пройти 51 чек хвилі 173 і всю зону AD-RBAC, поки крутяться S6 —
   перепровірка не чекає кінця всього плану.
